# v1.1.13 候选第二轮修复审计

基线：`main` 的 `63faec4`。本轮只推送源码到 `main` 并等待 CI 更新 `preview-main`；`package.json` 保持 `1.1.12`，不创建发布说明、标签或 Release。浏览器数据全部来自本地 mock，无真实站点数据。

## H～K：复现、修复与测试

| 项目 | 修改前复现 | 修改后验收 | 实现与测试 |
|---|---|---|---|
| H 页面配置门禁 | 原有 `configReady()` 纯函数测试全绿，但不能证明三个页面真的接线 | 首页、详情、设置页的 pending / 成功 / 失败 / 已有配置刷新由 SSR 页面渲染测试覆盖；逐处反向删门禁均使相应测试失败，随后手工恢复 | `tests/config-page-wiring.test.ts`；页面门禁原行为保留，详情门禁由 I 补主后端条件 |
| I 多后端详情 | 新增测试在旧实现下失败：B 配置已就绪、A 仍加载时详情提前解除骨架屏 | 同时等待 owning B 与主后端 A 确定；A 成功时指标卡采用 A 的预设、标题仍为 B；A 失败时用默认设置；单源复用已有主配置 | `src/views/ServerDetailView.vue`；`tests/config-page-wiring.test.ts` 的多源等待、主配置样式、主配置失败与单源用例 |
| J 累计流量部分标记 | 原测试把部分合计单位断言为 `GB`，卡面没有可见标记 | 部分合计单位为 `GB · 部分`，详细缺失数仍留在 tooltip；完整合计仍为 `GB`，全缺失仍隐藏卡片；仅该卡的标记不截断 | `src/domain/theme-presentation.ts`、`src/components/dashboard/OverviewCards.vue`、`src/styles/main.css`；`tests/homepage-fidelity.test.ts`、`tests/overview-value-row.test.ts` |
| K 冷启动明暗 | 新增测试在旧实现下失败：已保存 dark 暂存仍从默认浅色起步、成功配置不写暂存 | 初始化即读独立且已校验的暂存；本地覆盖优先；成功配置后用后端模式覆盖并写暂存，失败时本次回默认但保留暂存；异常存储安全退化；设置三层与快照不受影响 | `src/theme/site-theme-hint.ts`、`src/stores/theme-settings.ts`、`src/App.vue`；`tests/theme-settings.test.ts`、`tests/site-theme-hint-wiring.test.ts` |
| 背景补修（用户追加） | 自定义背景配置延迟 1.5 秒时，默认图先在约 121ms 请求，约 1641ms 才切自定义图 | 配置未确定时不挂载默认图片；成功直接显示自定义图，失败才回退默认图；本地浏览器复测自定义场景只有自定义资源请求 | `src/components/dashboard/DynamicBackground.vue`、`src/stores/theme-settings.ts`；`tests/background-config-gate.test.ts` |

J 仅修改了已有“多节点仅合计双向完整的流量”用例的 `unit` 断言：`GB` → `GB · 部分`，因为部分合计不能只靠点击 tooltip 才得知；完整数据的 `GB` 断言和全缺失隐藏断言原样保留。其它既有断言没有删除或放宽。

### H 反向验证

均在工作区临时改动、运行 `tests/config-page-wiring.test.ts` 的定向用例后，用补丁手工恢复；最终文件里不存在临时删门禁代码。

| 临时去掉的门禁 | 失败用例 |
|---|---|
| 首页 `initialLoading` 的 `!siteConfigReady` | `home keeps dependent regions behind skeleton`（idle、loading） |
| 首页公告的 `siteConfigReady` | 同上（idle、loading） |
| 首页管理入口的 `!siteConfigReady` | 同上（idle、loading） |
| 详情 owning config 的 `configReady` | `detail keeps metrics behind skeleton while owning config`（idle、loading） |
| 详情主配置的 `configReady` | `multi-source detail waits for primary theme config after owning source is ready` |
| 设置页统计区的 `siteConfigReady` | `settings hides form and stats while config`（idle、loading） |
| 设置页表单前骨架屏的 `!siteConfigReady` | 同上（idle、loading） |

### J 响应式实测

本地 mock 临时把 10 台节点中的第 1 台 `net_tx` 设为 `null`，其余节点双向流量完整；测量后已恢复 mock 文件。以下宽度是浏览器视口宽，浏览器垂直滚动条使内容宽分别为 380 / 758px。

| 视口 | “部分”单位 client/scroll | 数值 client/scroll | 卡片高度：部分 / 完整 | 页面 scroll/client |
|---|---:|---:|---:|---:|
| 390px | 44 / 44px，右边缘在卡片内 | — | 80 / 80px | 380 / 380px |
| 768px | 44 / 44px，右边缘在卡片内 | 41 / 41px | 96 / 96px | 758 / 758px |

768px 初测时“部分”虽可见，但数值 38 / 49px 被截断；只给部分累计流量卡在 768～900px 缩小数值和单位，复测两者均完整且未撑高卡片。其它卡片样式未改。

### K 本地浏览器时间线

在独立的 `localhost:4182` origin、配置延迟 1500ms 的 mock 中，临时模拟北京时间白天（`getUTCHours() = 2`）；system 场景另模拟系统深色。mock 的 HTML 临时埋点记录 `performance.now()` 和 `data-theme` 变化，测后已移除埋点。以下毫秒均相对本次导航起点：

| 场景 | `data-theme` 记录 |
|---|---|
| 清空存储后首次访问 dark | 90.7ms `light` → 1659.8ms `dark` |
| 再次访问 dark | 82.7ms `dark`，没有浅色阶段 |
| 站点改 light | 78.0ms `dark` → 1636.6ms `light`，只切一次 |
| 再次访问 light | 138.7ms `light`，没有深色阶段 |
| 站点改 system、模拟系统深色 | 81.1ms `light` → 1635.0ms `dark`，只切一次 |
| 再次访问 system | 83.5ms `dark`，没有与系统相反的阶段 |

首次访问 dark 那次，`data-theme=light` 于 90.7ms 生效，浏览器 `first-paint` 为 132.0ms、`first-contentful-paint` 为 164.0ms：JS 生效比首次绘制早约 **41.3ms**，本次测量没有“首次绘制后、JS 生效前”的可见空窗。不同设备和缓存条件可能不同；HTML 未注入生产脚本，JS 执行前的时段不能由主题保证。

### 自定义背景：测量与用户追加修复

本地 mock 临时令 `backgroundEnabled=true`，明暗自定义 URL 都指向 mock 的 SVG，配置延迟 1500ms；仅此测量采用虚构路径，不含真实站点地址。修复前：默认背景约 120.7ms 发请求、127.9ms 显示；自定义背景约 1640.6ms 显示、1641.1ms 请求。修复后：约 100.9ms 为空背景层，1696.2ms 直接进入自定义背景，资源列表只有自定义 SVG（1696.5～1703.8ms），没有默认图请求。配置明确失败的对照：约 1632.4ms 显示默认背景，1634.6ms 才请求默认图。临时 mock 改动与埋点均已移除；没有向生产 bundle 添加测试代码。

## 保留待决定事项

- `preview-main` 历史增长与匿名化残留未在本轮清理或重新审计；本轮仅记录，不把它们混入 H～K 修复。
- 发布仍待审计和用户确认。本轮不修改版本号，不发布 v1.1.13。
