# 详情页历史图表对照与颜色修复（第 15 轮）

基准：Komari Glassmorphism `bf8376587c720de915ac48789a8a180357c762d6`
本轮起点：`v1.1.0-test.5` / `961a4edb1683e72b6bd615e3d0b8c69783d4eccc`

> **第二阶段 Test 2 起**：详情页图表已按上游 `LoadChart` / `PingChart` 重建，`HistoryChart.vue` 已移除。
> 下文的逐图对照表，以及「一个组件覆盖全部分族」的描述，只反映第 15 轮的状态。
> 现行对照见 [`phase2-test2.md`](phase2-test2.md) 第 2 节。CHART-001 / 002 的根因结论（canvas 不解析 CSS 变量）仍然有效。

## 先确定每张图对应哪个上游组件

上游详情页（`views/InstanceDetail.vue`）只挂两个图表组件：`LoadChart` 与 `PingChart`。
其中 `LoadChart` 内部又分成两条路径：

| 上游路径 | 覆盖的图 | 配色来源 | 线宽 / 线帽 | 图表形态 |
|---|---|---|---|---|
| `LoadChart.vue` 内联 `VChart` | CPU、内存/Swap、磁盘、网络、GPU、连接数、进程 | `getLoadChartPalette()` + `chartThemeColors`（**均为具体色值**） | `1.5` + `cap: 'round'` | 折线 + `areaStyle` 渐变填充 |
| `LoadChart.vue` 里的 `MetricSeriesChartCard` | 累计与周期流量、GPU 显存、温度、Ping 延迟、Ping 丢包 | 调用方传入 `getChartSeriesPalette()`（具体色值）；**图表底色 / 文字**用 `var(--color-*)` | `1.6`，无 cap | 纯折线，无填充 |
| `PingChart.vue` | 独立的 Ping 面板 | `getChartSeriesPalette()` + `chartThemeColors` | `1.5` + `cap: 'round'` + `type: lineType` | 折线，`smooth` 可切换 |

本主题只有一个 `components/detail/HistoryChart.vue`，一套配置覆盖全部分族。
它移植自 `MetricSeriesChartCard`，因此**只能说与那一条路径同形**，
不能据此宣称所有详情图都已复刻到位。逐图对照如下。

> **第二阶段 Test 1 修订**：下表原先把 `LoadChart` 内联的图一概写成「带渐变填充」，
> 这是**不准确的概括**。逐条核对 series 配置后确认：只有 CPU、RAM、磁盘已用、进程数带
> `areaStyle`，**网络图两条线都没有填充**；并且内联图取色用的是
> `getLoadChartPalette()` 的角色板（primary / secondary / tertiary / quaternary /
> quinary / senary），不是 8 色序列板。逐序列映射与修复前后对照见
> [`phase2-test1.md`](phase2-test1.md) 第 4 节，本轮已按角色逐条对齐。

## 逐图对照表

| CFSM 图 | 上游对应 | 序列配色 | 线宽 / 线型 | 图例 | 坐标轴 / 网格 | tooltip | 指示线 | 差异说明 |
|---|---|---|---|---|---|---|---|---|
| CPU | `LoadChart` 内联 | ✅ 同一调色板 | 1.6（上游内联是 1.5+round cap） | ✅ | ✅ | ✅ | ✅ | 上游该图带 `areaStyle` 渐变填充，本主题是纯折线 |
| 系统负载 | `LoadChart` 内联 | ✅ | 同上 | ✅ | ✅ | ✅ | ✅ | 同上；上游 5/15 分钟线用 `dashed`，本主题标准模式统一实线 |
| 内存与交换分区 | `LoadChart` 内联 | ✅ | 同上 | ✅ | ✅ | ✅ | ✅ | 同上 |
| 磁盘容量 | `LoadChart` 内联 | ✅ | 同上 | ✅ | ✅ | ✅ | ✅ | 同上 |
| 实时网络 | `LoadChart` 内联 | ✅ | 同上 | ✅ | ✅ | ✅ | ✅ | 同上 |
| 累计流量 | `MetricSeriesChartCard`（`tone: sky`） | ✅ | ✅ 1.6 | ✅ | ✅ | ✅ | ✅ | 同形 |
| 磁盘 IO | — | ✅ 同一调色板 | ✅ | ✅ | ✅ | ✅ | ✅ | **CFSM 独有**，上游无磁盘 IO 历史图 |
| GPU | `LoadChart` 内联 GPU 卡 | ✅ | 同上 | ✅ | ✅ | ✅ | ✅ | 上游带填充；本主题按设备拆多条序列 |
| Ping 历史 | `MetricSeriesChartCard`（`tone: cyan`） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 同形；本主题按 8 个探测目标出序列 |
| 丢包历史 | `MetricSeriesChartCard`（`percent-scale`） | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | 同形 |

「上游带填充、本主题纯折线」是**记录在案的形态差异**，不是本轮修复项：
它来自「一个组件覆盖全部分族」的既有结构选择，改动会波及第 13 轮已验收的详情几何。

## CHART-001 · 折线全黑（已修复）

**现象**：多条历史折线呈黑色，与 tooltip 色点、底部摘要色点的颜色不一致。

**根因**：ECharts 走 `CanvasRenderer`，序列颜色最终落到 canvas 2D 的 `strokeStyle`，
而 **canvas 不解析 CSS 变量**——赋一个 `var(--emerald)` 会被直接丢弃，
线条留在上一次的绘制样式上。本主题此前在
`domain/server-detail.ts` 的 `COLORS` 里写的就是 `var(--emerald)` / `var(--blue)` /
`var(--amber)` / `var(--violet)` / `var(--red)`，同一批值又用于
HTML tooltip 的色点与底部摘要色点——HTML 一侧能解析、canvas 一侧不能，
于是出现「折线全黑但色点正常」这种前后不一致。

**像素证据**（1440×900 / 浅色 / 本地生产构建，`getImageData` 直接读画布）：

| 区域 | 修复前 | 修复后 |
|---|---|---|
| 绘图区（9 张图逐张取样） | 只有 `0,0,0` | `#FF6B6B` / `#4ECDC4` / `#A78BFA` / `#60A5FA` / `#FFB347` |
| 底部 HTML 摘要色点 | `rgb(15,159,120)` 等（正常） | 与绘图区**逐一相等** |

**修复**：新增 `src/utils/chart-palette.ts`，逐字移植上游 `utils/chartPalette.ts` 的
`DEFAULT_SERIES_PALETTE` / `ACCESSIBLE_SERIES_PALETTE` / `ACCESSIBLE_LINE_TYPES`，
另按上游 `LoadChart` / `PingChart` 的 `chartThemeColors` 移植一份 `getChartThemeColors(dark)`。
调色板由 `ServerDetailView` 按色觉设置派生后传进两个构建器（对应上游 `LoadChart`
把 `chartColors` 传给图表卡的做法），**在统一的配置层处理，没有逐图打补丁**。

同一批取样在上游 Komari 详情页得到同一组色值（`255,107,107` / `96,165,250` /
`167,139,250` / `78,205,196`），确认配色来源一致。

## CHART-002 · 浅色模式图内文字偏白（已修复）

**现象**：浅色模式下图表内部的图例文字偏白，可读性不足。

**根因**：与 CHART-001 同源。图例、坐标轴标签、网格线也全用 `var(--muted)` /
`var(--line)`，canvas 无法解析后落到无效样式。

**像素证据**（同上环境，带 alpha 采样）：

| 区域 | 修复前 | 修复后（浅色） | 修复后（深色） |
|---|---|---|---|
| 图内图例文字 | 不透明 `255,255,255`（白底上几乎不可见） | `0,0,0@α≤140` = `rgba(0,0,0,0.55)` | `255,255,255@α≤140` = `rgba(255,255,255,0.55)` |
| x / y 轴标签 | 纯黑 `0,0,0@255` | `rgba(0,0,0,0.55)` | `rgba(255,255,255,0.55)` |

上游同位取样同样是 `0,0,0@α≤140`，两版一致。

> 图例条带里仍有约 26 个不透明白像素：那是 ECharts 折线图图例小标记中心的填充圆点，
> 上游同位取样同样存在，属上游同等行为。

## CHART-003 · 悬浮网络图曲线消失（未复现，记录复现条件）

**结论：在本轮候选构建中未复现。**

复现尝试覆盖：1440×900 与 375×812、浅色、`实时网络`（两条序列）、
在绘图区 25% / 50% / 75% 三个横向位置悬浮、悬浮后移出。
每一步都直接读画布统计两条序列的像素数：

| 状态 | 序列像素数 |
|---|---|
| 未悬浮 | `#4ECDC4`×649、`#60A5FA`×528 |
| 悬浮 25% / 50% / 75% | 完全相同 |
| 移出 | 完全相同 |

即曲线没有被隐藏、没有被 blur 状态调暗、也没有被重绘成背景色。

可观察到的**相关事实**：tooltip 配置是 `confine: true`（上游 `LoadChart` 的
`baseTooltipConfig` 是 `confine: false`），气泡被限制在图表框内，
1440 下覆盖绘图区约 11%、375 下约 21%，会遮住一部分曲线。
这解释「看起来像消失」的观感，但不构成曲线本身消失。

同时要记录的是：**修复前**所有折线都是黑色，且颜色赋值无效意味着重绘时
canvas 会沿用上一次的绘制样式——这类「取决于绘制顺序」的表现本身就不稳定。
CHART-001 / 002 修复后颜色是固定字符串，重绘路径不再有不确定性。

> **待用户确认**：若在正式版实际应用后仍能观察到该现象，请提供触发时的
> 视口、主题、具体图表与序列数量，本轮的像素采样脚本可以直接复用。

## 交互与状态回归（本地生产构建，像素采样）

| 项 | 结果 |
|---|---|
| 首次加载 / 详情直达 / 刷新 / 返回首页后再进另一台 | 9 张图全部非黑，序列色恒为调色板色 |
| 时间范围切换（默认 → 6 小时 → 24 小时） | 颜色不变，无黑线 |
| 浅色 ↔ 深色运行时切换（不刷新） | 坐标轴文字由 `rgba(255,255,255,·)` 变 `rgba(0,0,0,·)`，**无旧颜色残留** |
| 色觉友好模式 | 序列色整体切到上游无障碍板（`#0072B2` / `#E69F00` / `#CC79A7`…），HTML 色点同步；线型按 `solid`/`dashed`/`dotted` 轮换 |
| 六档视口 × 浅色 / 深色 | 9 张图、0 条黑线、0 个调色板外颜色；HTML 图例无裁剪 |
| 控制台 | 无错误（仅有本轮采样脚本自身触发的 `getImageData` 性能提示） |

> **证据缺口（单列）**：Browser 面板在本次会话中处于隐藏状态，截图返回空白，
> 因此本轮视觉证据一律采用 `getImageData` 直接读画布像素（比截图更强，
> 但没有可供肉眼比对的图片）。**图例点击开关**未能用合成指针事件驱动
> （zrender 收到了悬浮事件——emphasis 高亮色确实变化过——但 click 未触发切换），
> 该项因此只做了结构性验证：图例颜色与序列同源、option 中颜色为静态字符串，
> 开关不会重新引入变量解析问题。真实点击开关留待实际使用验证。

## 数据语义未被改动

修颜色没有碰数据层：`connectNulls: false` 保留，probe 的 `false`（未配置）与
`null`（超时）都不进入数值 series、不写成 0、不插值，时间戳按真实值升序落点，
source / apiBase 归属不变。相关断言在 `tests/chart-colors.test.ts` 的
「修颜色没有动数据语义」一节。

## v1.1.7 · 延迟大图的「隐藏尖峰」（已发布）

**定位**：用户批准的可选绘图扩展，不是上游移植。上游的「平滑峰值」会用 EWMA 改写数值、填补空洞，而且始终做线性插值；本主题不照搬。检测规则、参数与剩余限制记在 `docs/todo.md` TODO-01，这里只记图表层面的对照与验证。

**四种开关组合**（同一份原始数据）：

| 曲线平滑 | 隐藏尖峰 | `smooth` | 绘图数组 | 纵轴 |
|---|---|---|---|---|
| 关 | 关 | 0.1 | 与 v1.1.6 相同，全部有效点保留 | 全量 |
| 开 | 关 | 0.6 | 全部有效点保留 | 全量 |
| 关 | 开 | 0.1 | 命中点写成 null 占位，其余逐点相等 | 按可见数据收缩 |
| 开 | 开 | 0.6 | 与上一行是同一组遮蔽点 | 按可见数据收缩 |

- 四种组合的数组长度与时间映射相同，`connectNulls` 始终为 `false`。
- 遮蔽集合只由原始数据决定，与平滑无关；先开哪个开关，最终选项完全相同。
- 纵轴本来就没有写死上下限，遮蔽后由 ECharts 按仍可见的数据取整；所有线路共用一个纵轴。
- 任务卡统计始终从原始历史计算。

**自动化**：
- `tests/spike-mask.test.ts`（26 项）：检测规则的固定样本，覆盖任务书点名的全部形态。8 条规则逐条做过「去掉即失败」的检查。
- `tests/ping-spike-chart.test.ts`（24 项）：真实图表选项的四种组合、顺序无关、输入不被修改、统计不变、计数跟随任务选择与图例、悬浮提示的「已隐藏」，以及组件接线。

**浏览器验证**（本地生产构建）。固定样本 `history=spike` 共 48 个点：
- 电信约 200ms，在第 14、33 点各有一个孤立高点（1200 / 1400ms）；
- 联通稳定在约 600ms；
- 移动约 150ms，第 9 点是原始缺失，第 24–26 点是一段约 900ms 的平台。

| 项 | 结果 |
|---|---|
| 开启隐藏 | 按钮变为「隐藏尖峰 · 2」；纵轴上界 1,500 → 1,000；电信两处留下断口；移动的平台与原始缺口保留 |
| 统计 | 四种组合下任务卡同为 246ms / 600ms / 198ms（电信的平均值仍包含两个高点） |
| 请求 | 静置 6 秒无请求；开关连续切换 8 次，接口请求 0 个 |
| 顺序与恢复 | 画布像素哈希：先平滑后隐藏 = 先隐藏后平滑；四种组合各不相同；全部关闭后与初始状态逐像素相同 |
| 计数 | 取消电信的任务卡或在图例里关掉电信，按钮变为「隐藏尖峰 · 0」，恢复后回到 2 |
| 悬浮 | 被隐藏的两个时刻显示「电信 已隐藏 1200 ms」与「电信 已隐藏 1400 ms」，其他线路照常显示；普通时刻显示原始值 |
| 断口与过冲 | 两处断口在开启平滑后仍断开；电信线条最高像素的纵坐标在平滑开关前后相同，断口两侧没有向上过冲 |
| 说明入口 | 键盘聚焦打开并建立 `aria-describedby`；Escape 关闭，焦点留在按钮上；点击说明不改变开关；失焦关闭。375 下模拟触摸：点按打开、点外部关闭、再点按钮关闭，与已上线的任务统计气泡行为一致 |
| 布局 | 1440 与 375、浅色与深色各拍四种组合，共 16 张，均无横向溢出；375 下这一行宽 193px，不换行 |

**证据缺口**：
- 触屏只用模拟的 pointer / click 事件验证过，没有真机证据。
- 「曲线平滑」在移动那段平台的起落处画出圆角和轻微下探，这是既有 `smooth: 0.6` 的行为，与隐藏尖峰无关，本轮没有改。

## v1.1.8 · 短时高值段与安静邻域（已发布）

**起因**：线上真实数据里出现两种 v1.1.7 漏判的形态，纵轴照旧被撑到很高。两者与时间窗口无关，1 / 6 / 12 / 24 小时都能出现。

| 形态 | 线上样本 | v1.1.7 | v1.1.8 |
|---|---|---|---|
| 相邻两个高点 | 绿云 · 移动，12 小时窗口，常态约 80ms，23:02 与 23:06 分别为 584ms 与 604ms | 整段保留，遮蔽后最大仍是 604ms | 整段隐藏，遮蔽后最大 101ms |
| 近旁有尖峰的孤立高点 | NETCUP · 电信，6 小时窗口，17:27 的 701ms 左侧紧跟着 1219 / 1217 两个高点 | 左侧中位数被抬到 718ms，判为阶跃而保留 | 取参考邻域时跳过疑似高点，左侧回到约 200ms，正常隐藏 |

**规则变化**：
- 检测从「单个孤立高点」扩展到「短时高值段」：连续不超过 2 个采样、且真实跨度不超过 10 分钟；连续三个及以上，或跨度超过 10 分钟，仍按持续高延迟保留。
- 取参考邻域时跳过其它疑似高点，每侧最多跳过 3 个；跳过的点不进入基线，也不参与两侧中位数比较。超过上限就当作证据不足，保留。
- 其余规则不变：缺口两侧不借证据、首尾保留、阶跃与爬升保留、整条线不会被遮蔽完。

**离线回放**（线上 `/api/history/all` 只读取样，5 台节点 × 1 / 6 / 12 / 24 小时）：

| 项 | 结果 |
|---|---|
| 高点命中 | 改动前有 2 处保留的高点段，改动后全部命中；各线路遮蔽后的最大值回到常态区间（例：绿云 · 移动 604 → 101，NETCUP · 电信 1206 → 229） |
| 平台与故障 | 连续三点及以上的平台、阶跃、爬升在回放中一处都没有被隐藏 |
| 固定样本 | 本地 `history=spike` 下仍是「隐藏尖峰 · 2」：移动的三点平台与原始缺口保留，任务卡统计仍为 246ms / 600ms / 198ms |

**「曲线平滑」已删除**：该开关只改折线曲率，不改数值，也与隐藏尖峰无关。v1.1.8 移除按钮与状态，曲率固定为上游未开启平滑时的 0.1；说明入口保留，只解释隐藏尖峰。

## v1.1.11 · 窗口次边缘尖峰（已发布）

**起因**：孤立高值位于时间窗口倒数第二格、右侧只积累到一个恢复正常的点时，旧规则只允许“正好位于最外侧”的高点缺少一侧证据，因此按钮仍会显示「隐藏尖峰 · 0」，纵轴继续被高值拉伸。

**规则变化**：

- 所选窗口的第二个或倒数第二个点允许使用边缘侧现有的一个样本，但另一侧仍须达到正常的最少证据数；
- 边缘侧已有样本时，必须与另一侧基线一致，真实阶跃仍然保留；
- 单点或相邻两点组成的短时高值段会整段隐藏，不会只去掉其中一个；连续三点以上仍按持续高延迟保留；
- `null`、离线占位或异常时间间隔造成的证据不足仍然不跨越，原始值、任务卡统计与 Tooltip 均不改变。

**匿名样本回归**：使用稳定基线、倒数第二格单个高值、末格恢复正常的合成序列回放；高值在右侧只有一个点时即命中，按钮计数为 1。补足后续历史再回放仍命中同一点；开启开关后只在绘图副本中留下断口。

**实时链路复核**：使用匿名化的协议场景确认首页同轮错峰消息只触发一次合计，单节点详情在首次历史批次后继续按 Angel 配置接收样本。此次没有改动首页聚合、详情订阅、实时缓冲或 Angel 上报节奏。
