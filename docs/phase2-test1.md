# 第二阶段 Test 1 报告

| 项目 | 值 |
|---|---|
| 起点正式版 | `v1.1.0`（tag 对象 `7a4156a`，解引用源码 commit `bec06b9`） |
| 起点后的 main 提交 | `70bec62` / `5b3fd1a` / `2a3f71f`（发布说明重写）、`c4ec5ba`（Release 正文同步 job），均为文档与 CI，无功能改动 |
| 本轮候选版本 | `v1.1.1-test.1`（`v1.1.1*` 在远端未占用） |
| Komari 基准 | `bf8376587c720de915ac48789a8a180357c762d6`（本轮 `git fetch` 复核，上游默认分支仍是该 commit） |
| 真实环境 | https://sr.706632.xyz |
| 实际 Worker 版本 | `2.8.5 Beta5`（页脚与 `/api/config` 双重确认） |
| 探针 Agent | `1.0.16`（详情页系统信息读出；本地 fixture 为 `1.5.2`） |

## 1. 线上版本与资产溯源

v1.1.0 的线上应用状态**不再只是「用户确认」**，本轮完成了独立资产核对：

| 资产 | 线上 | v1.1.0 发布包 | 结论 |
|---|---|---|---|
| `index.html` 引用 | `assets/index-CWXLo61-.css`、`assets/index-DxhZ0WpF.js` | 同上 | 文件名一致 |
| `assets/index-CWXLo61-.css` | SHA-256 `f5f1661e8cd5e1c9…` | 同 | **逐字节一致** |
| `assets/index-DxhZ0WpF.js` | SHA-256 `54b600ae1edc7319…` | 同 | **逐字节一致** |
| `index.html` 本身 | 摘要不同 | — | 差异仅为 CFSM 注入的站点标题 `养鸡场`、favicon 与 Cloudflare 挑战脚本，资源引用部分完全相同 |

即线上运行的就是 v1.1.0 的产物，**「资产独立核对未完成」这一条本轮关闭**。

## 2. v1.1.0 线上回归（只读）

全部在 https://sr.706632.xyz 实测，未修改任何后台配置、未重启探针、未迁移数据库。

| 项 | 线上实测 | 结论 |
|---|---|---|
| CHART-001 | 8 张历史图绘图区取样 0 张含 `0,0,0`；序列色与底部 HTML 色点逐一相等 | DEPLOYED-VERIFIED |
| CHART-002 | 坐标轴与图例文字 `rgba(0,0,0,α≤140)`，浅色下可读 | DEPLOYED-VERIFIED |
| F-01 | `.node-metric__value` = 12px / 500 / tabular-nums | DEPLOYED-VERIFIED |
| F-02 | `.node-probe__value` = 11px / 500 / normal | DEPLOYED-VERIFIED |
| F-04 | `.brand__copy strong` = 18px / 600 / 字距 normal | DEPLOYED-VERIFIED |
| F-05 / F-06 | 字体栈 `system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`；`font-synthesis` 为默认值、`text-rendering: auto` | DEPLOYED-VERIFIED |
| U-05 | **冷启动直达详情**（hard load）：`/api/servers` 恰好 1 次，节点选择器 5 项，8 张指标卡按站点开关正常显示 | DEPLOYED-VERIFIED |
| 第 14 轮窗口契约 | `/api/servers` 窗口 20 点、每点 8 key（`ct/cu/cm/bd/node_1..4`），`bd` 与 `node_1..4` 取值 `false` 未被显示成 0；首页探针柱 20 根 | DEPLOYED-VERIFIED |

一处观察（非缺陷，记录待 Test 2 评估）：冷启动详情页 `/api/config` 被请求 **2 次**（应用初始化一次 + 详情源配置一次）。不影响正确性，也非本轮引入。

> 方法学提醒：SPA 用 hash 路由切换时 `performance.getEntriesByType('resource')` 会累计首页的请求，
> 首次测得 `/api/servers` 3 次是这个原因。必须用 hard load 重测，本表数据取自 hard load。

## 3. CHART-003 悬浮曲线

**状态：NOT-REPRODUCED（覆盖条件已扩大，但仍未覆盖全部）。**

本轮用**真实指针**（非合成事件）在本地候选构建上覆盖：

| 条件 | 结果 |
|---|---|
| 1440×900 浅色，Ping 历史 4 序列，真实 hover | 四条曲线全部保留；tooltip 正常显示 `电信 41.0 ms / 联通 超时 / 移动 46.0 ms / Node 1 63.0 ms` |
| 1440×900 深色，同上 | 同上，曲线保留，tooltip 底色 `rgba(40,40,40,0.95)` |
| 真实点击图例隐藏 `电信` 后 | 该序列消失、图例标记转灰，其余三条不受影响——这是**正常的图例隐藏**，与「绘制丢失」是两回事 |
| 再次真实点击恢复 | `电信` 重新绘制（`#FF6B6B` 像素回归） |
| 悬浮移出 | 曲线像素不变 |

可复现的**相关事实**：tooltip 为 `confine: true`（上游 `LoadChart` 的 `baseTooltipConfig` 是 `confine: false`），
气泡被限制在图表框内，1440 覆盖绘图区约 11%、375 约 21%，会遮住部分曲线。
这能解释「看起来像消失」的观感，但不构成曲线本身消失，本轮**不据此改动 tooltip 配置**。

未覆盖：移动端真实触摸（浏览器面板在该时段不可见，动作超时）、实时更新过程中的长时间观察、多序列大量交叠。
留给 Test 2，并保持 NOT-REPRODUCED，不改判为 NOT-A-BUG。

## 4. DEFERRED-CHART-01 逐图对齐（已修复）

第 15 轮把上游内联图一概概括为「带渐变填充」。逐条读 `LoadChart.vue` 的 series 配置后，**这个概括是错的**：

| 上游图 | series | 角色色 | 线宽 / 线型 | areaStyle |
|---|---|---|---|---|
| CPU | CPU | primary `#FF6B6B` | 1.5 + round | **有** |
| CPU | 负载（第二根轴） | secondary `#FFB347` | 1.5 + round | 无 |
| 内存 | RAM | primary | 1.5 + round | **有** |
| 内存 | RAM 总量 | quinary `#60A5FA` | 1.2 dashed | 无 |
| 内存 | Swap | secondary | 1.5 + round | 无 |
| 磁盘 | 磁盘已用 | tertiary `#4ECDC4` | 1.5 + round | **有** |
| 网络 | 下载 / 上传 | quinary / quaternary | 1.5 + round | **两条都没有** |
| GPU | GPU 使用率 / 显存 | senary `#34D399` / quaternary | 1.5 + round | 无 |
| 进程 | 进程数 | quaternary | 1.5 + round | **有** |
| 累计流量（卡片） | 累计下载 / 上传 | quinary / quaternary | 1.6 无 cap | 无 |
| Ping / 丢包（卡片） | 按任务序号 | `getChartSeriesPalette()[i]` | 1.6 无 cap | 无 |

关键发现：上游内联图用的是 **`getLoadChartPalette()` 的角色板**，不是 8 色序列板。
第 15 轮只移植了序列板，于是「颜色都来自调色板」成立，但**与上游同名序列对不上**。

本轮新增 `getLoadChartPalette()`（逐字移植），并在 `domain/server-detail.ts` 按语义逐条映射：

| 本主题图 / 序列 | 修复前 | 修复后 | 上游对应 |
|---|---|---|---|
| 内存 RAM | `#4ECDC4` | `#FF6B6B` + 渐变 | RAM = primary + area |
| 内存 Swap | `#60A5FA` | `#FFB347` | Swap = secondary |
| 磁盘 Disk | `#A78BFA` | `#4ECDC4` + 渐变 | 磁盘已用 = tertiary + area |
| 实时网络 下载 / 上传 | `#4ECDC4` / `#60A5FA` | `#60A5FA` / `#A78BFA`，**无填充** | 下载 = quinary / 上传 = quaternary |
| 累计流量 接收 / 发送 | `#FF6B6B` / `#A78BFA` | `#60A5FA` / `#A78BFA` | 累计下载 / 上传 |
| CPU | `#FF6B6B` | `#FF6B6B` + 渐变 | primary + area |
| 系统负载 1 min | `#FF6B6B` | `#FFB347` | 上游负载线 = secondary |
| GPU 首条 | `#FF6B6B` | `#34D399` | GPU 使用率 = senary |
| 线宽 | 全部 1.6 | 内联族 1.5 + round cap，卡片族 1.6 | 分别对应两条上游路径 |

浏览器像素复核（1440 浅色，本地生产构建）：各图绘图区实色与底部 HTML 色点逐一相等；
半透明像素统计区分出填充与抗锯齿——CPU 16677、内存 23622、磁盘 23410（有填充），
实时网络 4635、累计流量 4023（仅抗锯齿，无填充），与上游配置一致。

**记录的必要差异**：磁盘 IO 是 CFSM 独有图，上游无对应组件，沿用公共视觉语言（卡片线形 + 角色板取色），不虚构对应关系。
语义口径未改动：内存/磁盘仍按百分比、负载仍单独成图——这些需要产品裁决，不借样式修复夹带。

## 5. DEFERRED-TYPO-01 行高与盒模型（已修复主卡片）

第 15 轮的结论是「单独注入上游行高会让卡片高度误差从 1.94px 扩大到 3.53px」，因此搁置。
本轮按任务书要求**成组推导**，先逐行测出差在哪：

| 节点卡内部 | 上游 | 修复前 | 修复后 |
|---|---|---|---|
| chips 行 | 19 | 18.84 | 18.84 |
| 指标网格 | 97.41 | **92** | **97.44** |
| 信息盒网格 | 45.41 | 45.41 | 45.41 |
| 探针网格 | 44 | **45** | **44** |
| 标签行 | 20 | **18.5** | **20** |
| 头部 | 44（`px-4 py-3`） | **24**（靠卡片级 14px 内边距近似） | **44** |
| 主体 | 285.81（`p-4 pt-0`） | 296.69 | **285.69** |
| 内容宽 | 269 | **273** | **269** |
| **卡片总高** | **331.81** | **333.75（+1.94）** | **331.69（−0.12）** |

成组改动（全部取自上游实测值，非推测）：

- 12px 文本行高 `normal` → `16px`，11px 提示行高 → `15.7143px`，标签高 → `20px`（上游 Badge `h-5`）。
- 卡片 `padding: 0`、`gap: 0`；头部 `12px 16px`、主体 `0 16px 16px`（照抄 `px-4 py-3` / `p-4 pt-0`）。
- 探针面板 `padding: 8px`、`gap: 6px`、柱区 `min-height: 11px`（上游 44 = 8+11+6+11+8）。
- compact 档补上上游 chips 行的 `-mt-1`（mini 档原本就有）。

未使用 `transform` / `scale` / 负 margin 去凑误差；负 margin 只在上游本来就有的那一处照抄。

**其他视口复核**（证明未恶化）：

| 视口 | 卡片 | 总览卡 | 横向溢出 |
|---|---|---|---|
| 375×812 | `[16,536,343,331.69]`（第 11 轮记录 x/y/宽相同，高由 333.75 改善） | `109×80` 不变 | 无 |
| 768×1024 | `[16,377,357,331.69]` | `114.33×96`（与上游同宽度实测相同） | 无 |
| 1440×900 | `[91,337,303,331.69]` | `201.33×96` 不变；控制条 `[91,289,1248,32]` 不变 | 无 |

**剩余方案（留 Test 2）**：`comfortable` / `large` 两档仍沿用旧的卡片级内边距，本轮未动；
详情页与首页其余组件的行高仍是 `normal`。

## 6. 正式版证据缺口的补齐情况

| 缺口 | 本轮结果 |
|---|---|
| 截图空白、只能靠画布采样 | **部分补齐**。浏览器面板可见时截图正常，已取得线上首页/详情、候选构建首页/详情（浅色与深色）的真实页面截图。**已知限制：页面滚动后截图返回纯背景色**，因此图表截图通过开启详情分区 Tab 把图表放到首屏来取得；面板被隐藏时所有需要绘制的动作（截图/悬浮/点击）会超时。 |
| Rendered Fonts 不可用 | **未补齐**。仍只能用同浏览器 Range 宽度指纹判定字族，结论限于本机（中文 Windows，`system-ui` = 微软雅黑）。保持「证据有限」，不宣称已证实字体完全一致。 |
| 图例点击只做结构验证 | **已补齐**。真实指针点击图例可隐藏并恢复序列，双向均有截图与像素证据。 |

## 7. D-01 与未确认数据项

- **D-01**：维持显示最新上报值，未切换为窗口平均。本轮复核线上标量与历史来源一致、`null`/`false`/缺失未变成 0。
  未新增占布局的固定说明文字，也未新增上游没有的交互，因此**口径歧义仍未消除**，D-01 保持未解决。
  未把逐桶采样点均值称作按探测次数加权的丢包率。
- **探针首轮空串转 0**：线上 `/api/servers` 全量响应中 `ping_*` 标量为 0 的出现次数为 **0**。
  稳态部署无此现象**不等于**首轮问题不存在；需要隔离环境验证，本轮未授权重启生产探针。保持 NEEDS-EVIDENCE。
- **node_1..4 长窗口 History 聚合**：线上窗口确有 8 个 key，但 `node_1..4` 全为 `false`，
  只证明 schema 兼容，**不证明**已配置目标的采样与聚合正确。保持 NEEDS-EVIDENCE，交 Test 2。

## 8. 质量门

- lint 通过；typecheck 用仓库外便携 **Node v22.12.0** 跑 `vue-tsc`（确认分析 SFC 模板），Bun `1.3.14`。
- 25 个测试文件、**251 个测试**全部通过（新增逐序列映射、面积填充、线宽线帽、节点卡盒模型断言）。
- build 通过；`dist validation passed: index.html and 22 asset file(s)`；`git diff --check` 干净。
- 未删除测试，未修改 CI 或依赖预算来规避失败。
