# 财务币种与汇率：CFSM 兼容审计（v1.1.7）

本文件回答两个问题：CFSM 实际给了什么数据、按什么语义；Komari Glassmorphism 固定基准（`bf83765`）在这些数据上做了什么。结论决定本主题的适配方式。实现结果与验证记录在文末。

## 0. 审计基线（2026-09-17）

| 项 | 记录 |
|---|---|
| 本主题 HEAD | `bee08711d108b7f1ecc9542a6cd4c31375890661`（main，已推送） |
| 前一检查点 | 隐藏尖峰 + 控件配色的候选：源码 `bee0871`，产物 `e2cfa231106b894821b7c383a6e88f398ccedb0a`，分支 `candidate-bee0871`（保留） |
| Latest / theme-dist | v1.1.6 / `55ea055`（= `theme-v1.1.6`） |
| `v1.1.7` / `theme-v1.1.7` | 均不存在 |
| 线上实际应用 | `https://sr.706632.xyz` 入口 `assets/index-CJmOtUgz.js`，与 `theme-v1.1.6` 逐字节相同；v1.1.7 候选尚未应用 |
| 线上 CFSM | `/api/config` 返回 `version: "2.8.5 Stable"` |
| CFSM 源码 | 2.8.5 Stable = `1dc0dc49b0e954fa325f1a95f88eaaa1c8fab8bf`；任务书引用的 `428d2bef4bb06725debfc3053883cb84a61fa480` 是其后 9 个提交（2.8.6 Beta1 起的流量报告）。两者之间 `src/frontend/utils/finance.js`、`src/frontend/utils/server.js`、`src/utils/serverBilling.js` 均无改动；`handlers/dashboard.js` 只多删了一个私有字段 `traffic_snapshots`。因此部署版本与引用版本的财务契约相同 |
| Komari 基准 | `bf8376587c720de915ac48789a8a180357c762d6` |

## 1. CFSM 的真实数据契约

### 1.1 接口返回（线上只读实测）

`/api/servers` 与 `/api/server?id=` 返回同一组财务字段，全部是字符串：

| 节点 | `price` | `currency` | `billing_cycle` | `expire_date` | `auto_renewal` |
|---|---|---|---|---|---|
| DMIT | `"36.90"` | `"$"` | `"year"` | `"2026-11-27"` | `"1"` |
| V.PS | `"69.98"` | `"€"` | `"five_years"` | `"2031-03-21"` | `"1"` |
| 绿云 | `"74.00"` | `"$"` | `"three_years"` | `"2028-07-03"` | `"1"` |
| NETCUP | `"11.88"` | `"€"` | `"year"` | `""` | `"1"` |
| Raksmart | `"1.99"` | `"$"` | `"month"` | `"2026-09-23"` | `"1"` |

站点开关只在 `/api/servers` 顶层的 `sysConfig`（`show_price` / `show_expire` / `show_tf`）；`/api/server` 的 `sysConfig` 只有 `long_history_points`。

**`show_price` 关闭时服务端不删价格字段**（`handlers/dashboard.js` 的 `withoutPrivateServerFields` 只删 bandwidth / note / auto_update / traffic_snapshots），隐藏完全由前端负责。

### 1.2 写入时的规范化（后端源码）

`handlers/admin.js` 的 `normalizeServerBillingData`（新增与编辑都经过它），以及旧数据迁移 `database/updateDatabase.js`：

| 字段 | 存储值 | 依据 |
|---|---|---|
| `price` | `normalizePrice`：空 → `""`；取文本里第一个数字；`-1` → `"-1"`；其它负数与非数字 → `""`；其余 → 两位小数 | `serverBilling.js` |
| `billing_cycle` | `normalizeBillingCycle`：8 个枚举之一；空或无法识别 → `"month"` | 同上；列默认值 `'month'` |
| `currency` | `normalizeCurrency`：后台 36 个选项里的符号；输入为空时存 `"¥"`；输入无法识别时存 `""` | 同上；列默认值 `'¥'` |
| `expire_date` | 表单日期 `YYYY-MM-DD` 或 `""`；`auto_renewal = 1` 时保存即顺延，每日定时任务也会在到期前一天顺延并写回 | `renewExpireDateIfNeeded`、`services/notification.js` |

因此正常数据里：价格是 `""` / `"-1"` / `"0.00"` / 两位小数；周期是 8 个枚举之一；币种是后台选项符号，`""` 只可能来自无法识别的输入。

### 1.3 CFSM 前端的语义（`finance.js` / `server.js`）

- **免费**：`isFreePrice` = 规范化后为 `"-1"` 或 `"0.00"`；汇总另外排除带 `白嫖中` 标签的节点。
- **周期天数**：月 30、季 90、半年 180、年 365、两年 730、三年 1095、四年 1460、五年 1825；未知周期按月处理。
- **月均**：`价格 ÷ 周期天数 × 30`。
- **剩余价值**：`min(价格, 价格 × 剩余毫秒 ÷ 周期毫秒)`；到期超过 100 年视为全价；已过期为 0；没有到期日时不计入剩余价值，另计「未设到期」数量。
- **币种**：`CURRENCY_ALIASES` 把后台符号与 ISO 代码映射到 43 种币种；无法识别时回退 CNY。
- **汇率**：
  - 先请求 `api.frankfurter.dev/v1/latest?base=CNY`，再请求 `open.er-api.com/v6/latest/CNY`，超时 5 秒；
  - 按本地日期缓存到 `cfsm_finance_exchange_rates_cny_v1`；
  - 返回表里缺的币种用内置表补齐，整份仍标为 `network`；
  - 显示币种为 6 种：CNY / USD / HKD / EUR / GBP / JPY。
- **展示**：只有 `show_price` 开启时才显示剩余价值卡；汇率在页面加载时就会请求，与该开关无关。

后台 36 个币种选项里，`kr` 的说明是「克朗（SEK/NOK/DKK）」，本身有歧义；CFSM 前端把它按 SEK 计算。

## 2. Komari 固定基准的行为

| 位置 | 行为 |
|---|---|
| `financeHelper.ts` | 12 种币种（CNY USD HKD EUR GBP JPY RUB CHF INR VND THB CAD），CNY 基准；每日缓存 `komari_finance_exchange_rates_cny_v1`；手动覆盖 `komari_finance_exchange_rate_overrides_v1`；显示币种 `fin_currency`（默认 CNY）；排除免费 `fin_exclude_free`（默认开）；未知币种按 CNY；缺汇率时 `rate \|\| 1` |
| 汇率来源 | `open.er-api.com/v6/latest/CNY`，其次 `api.frankfurter.app/latest?from=CNY`；12 种缺任何一种整张表作废；状态四种：今日网络汇率 / 今日缓存汇率 / 历史缓存汇率 / 内置参考汇率 |
| 剩余价值 | 剩余天数向上取整 ÷ 周期天数，**不封顶**；无到期日或已过期为 0（计入合计） |
| 首页总览 `NodeGeneralCards` | `remainingValue`（剩余价值，可点击打开明细，tooltip「总价值」）、`monthlyCost`（月费用估算）、`yearlyCost`（年费用估算），均按显示币种；未登录隐藏价格时显示 `***` 且不能点开 |
| 卡片预设 | 基础：memory / disk / **remainingValue** / totalTraffic / uploadSpeed / downloadSpeed；财务：remainingValue / monthlyCost / yearlyCost / expiringNodes / totalTraffic / trafficQuota；完整：全部 |
| 快捷筛选 `monthlyCost` | 预设里列出，但 `HomeView` 渲染前把它过滤掉了（`homeQuickControlOrder.filter(key => key !== 'monthlyCost')`），运行时不可见 |
| 详情 `InstanceDetail` | 节点价格与月均支出保留原币；剩余价值按显示币种换算；取汇率时**没有**套用手动覆盖 |
| 节点卡 / 列表 | 价格与剩余价值保留原币 |
| 明细弹窗 `FinanceDetailsDialog` | 标题「价值与费用明细」；三栏汇总（剩余价值 / 固定月均支出 / 当前节点估算）；页签：固定账单 / 按量估算 / 汇率设置；「排除免费节点」；固定账单表（节点 / 固定费用〔原币〕 / 到期 / 剩余价值 / 月均支出〔显示币种〕）；汇率页：来源 · 更新时间 · 1 CNY 对应数值、显示币种、「恢复今日汇率」（清除手动覆盖，恢复已取得的当日表，不触发网络请求）、12 个输入框（CNY 禁用） |

### 汇率服务核验（2026-09-17）

| 地址 | 结果 |
|---|---|
| `https://open.er-api.com/v6/latest/CNY` | 200，`access-control-allow-origin: *`，166 种币种（覆盖本主题需要的全部），带 `time_last_update_utc`，`cache-control: max-age=3600` |
| `https://api.frankfurter.dev/v1/latest?base=CNY` | 200，允许跨域，29 种（欧洲央行），缺 RUB / VND / BDT / PKR / LKR / MNT / UAH / KZT / NGN / EGP / AED / SAR / GTQ，`date` 为上一交易日 |
| `https://api.frankfurter.app/latest?from=CNY`（上游备用地址） | 301 跳转到 frankfurter.dev，跳转响应不带跨域头 |
| 线上 CSP（`sr.706632.xyz`） | `connect-src` 包含 `https://api.frankfurter.app`、`https://api.frankfurter.dev`、`https://open.er-api.com`，这三个都允许；其它汇率地址会被拦截 |

## 3. 字段 / 能力对照与适配方式

| 字段 / 能力 | CFSM 实际契约 | 当前主题 | Komari 期望 | 本次适配 | 证据 |
|---|---|---|---|---|---|
| `price` | `""` / `"-1"` / `"0.00"` / 两位小数 | `Number()` 解析；0 与 -1 显示「免费」；负数、非数字显示 `—` | 数字；-1 免费；0 显示免费 | 共享解析：空 → 未设置；-1 / 0 → 免费；正数 → 付费；其余 → 无效。未设置与无效都不算免费，也不计入合计；无效价格在合计说明里注明 | 1.1、1.2 |
| `currency` | 后台符号；`""` = 无法识别 | 原样拼在金额前 | 符号或代码；未知当 CNY | 按 CFSM 后台选项与 ISO 代码映射；`""`、`kr`、其它未知值标为「币种不可换算」，不当 CNY | 1.2、1.3 |
| `billing_cycle` | 8 个枚举 | 同一张天数表；未知 → 月均「不适用」、剩余价值不可用 | 天数 | 沿用本主题：未知周期不计算。CFSM 前端按月处理，但后端不会存未知值，只有手工改库才会出现 | 1.2、1.3 |
| `expire_date` | `YYYY-MM-DD` 或 `""` | 按 UTC 正午解析；无效 → 不可用 | dayjs 本地时间 | 沿用本主题口径；缺失与无效到期**不算已过期**，不计入剩余价值合计，单独计数 | 1.3、第 4 节样本 |
| 剩余价值 | 毫秒比例，封顶；>100 年全价 | 同 CFSM（UTC 正午） | 向上取整天数，不封顶 | 沿用本主题（与 CFSM 同口径），换算只在最后一步 | 第 4 节 |
| 月均支出 | 价格 ÷ 天数 × 30 | 同 | 同 | 不变 | 1.3 |
| 性价比月费 | —— | 价格 ÷ 月数（年 = 12） | —— | **不改**：分币种相对排名，与财务合计是不同消费者 | `domain/advanced-tools.ts` |
| 免费节点 | `-1` / `0.00`；汇总另排除 `白嫖中` | 0 / -1 | -1 或 `白嫖中` 标签；「排除免费节点」默认开 | 免费 = 价格 -1 / 0，或带 `白嫖中` 标签；「排除免费节点」默认开 | 1.3、2 |
| `show_price` | 服务端不删字段 | 详情整卡隐藏；节点卡、列表不显示价格 | 无此开关 | 新增的总览卡、明细、tooltip、可访问标签一律不出现价格；站点关闭价格时卡片不渲染，也不请求汇率 | 1.1 |
| 未登录隐藏价格 | —— | 详情整卡隐藏；节点卡不显示 | 总览卡显示 `***`，不能点开 | 总览卡显示 `***`，不能点开，不请求汇率；详情沿用现有「整卡隐藏」 | 2 |
| 详情冷启动（站点开关未知） | —— | 先隐藏 | —— | 不变；换算只在开关确认可见后进行 | `ServerDetailView.vue` |
| 显示币种 | 6 种，本地保存 | 无 | 12 种，本地保存，默认 CNY | 下拉沿用上游 12 种，默认 CNY；来源币种覆盖 CFSM 后台选项 | 1.3、2 |
| 汇率 | 每日缓存，补齐后仍标网络 | 无 | 每日缓存 + 手动覆盖 | 沿用上游模式；逐币种记录来源；补齐与参考值单独标明 | 第 5 节 |
| 首页财务卡 | 剩余价值卡 | 无（此前因不能换算而删去） | 剩余价值 / 月费用 / 年费用 | 恢复三张卡与上游预设；按显示币种；部分节点不可换算时标为「部分」 | 2 |
| 快捷筛选 `monthlyCost` | —— | 未列出 | 预设列出但运行时过滤掉 | 保持不显示（与上游运行结果一致），更正源码注释里的原因 | 2 |
| 明细弹窗 | 无 | 无 | 见第 2 节 | 移植固定账单、汇总与汇率设置；**不含**按量估算页签及其汇总栏 | 任务书 §4 |
| 快照导出 / 性价比 | —— | 已有 | —— | 不改格式与算法 | 任务书 §7 |

## 4. 固定样本（now = 2026-09-17 12:00 UTC，原币）

| 节点 | 月均 | 本主题剩余价值 | CFSM | Komari（UTC+8） |
|---|---|---|---|---|
| V.PS €69.98 / 五年，2031-03-21 | 1.1504 | 63.1162 | 63.0970 | 63.1162 |
| DMIT $36.90 / 年，2026-11-27 | 3.0329 | 7.1778 | 7.1273 | 7.1778 |
| 绿云 $74.00 / 三年，2028-07-03 | 2.0274 | 44.2648 | 44.2311 | 44.2648 |
| NETCUP €11.88 / 年，无到期 | 0.9764 | 不可用 | 0（另计未设到期） | 0 |
| Raksmart $1.99 / 月，2026-09-23 | 1.9900 | 0.3980 | 0.3648 | 0.3980 |

差异只来自到期日的解析时刻：本主题取 UTC 正午，CFSM 取 UTC 零点，Komari 取本地零点并按整天取整，金额相差在几分以内。本轮沿用本主题已验收的口径。

## 5. 汇率的适配规则

- **换算公式**：`目标金额 = 原金额 ÷ 源币种汇率 × 目标币种汇率`，CNY 固定为 1。源、目标币种相同时不需要汇率；任一汇率不是有限正数时不换算，也不显示金额。
- **精度**：内部保持完整精度，只在展示时舍入；合计直接对数值求和，不解析格式化后的字符串。
- **请求**：
  - 默认源为 `open.er-api.com`，备用源为 `api.frankfurter.dev/v1/latest?base=CNY`。上游的 frankfurter.app 地址已失效（301 跳转且不带跨域头），因此改用 CFSM 同款地址，线上 CSP 允许。
  - 单源超时 5 秒；失败后本页会话内不再重试，下次打开页面再试。
  - 同一页面共享一个进行中的请求。
  - 只有在价格可见、并且确实需要换算时才请求。
  - 请求不带凭据，也不发送任何节点信息。
- **缓存**：本主题自己的命名空间，带版本号；只保存数据源实际返回的汇率，并记录取得时间、数据源自带的日期和提供方。数据源缺的币种在使用时才由参考表补上，并标为「内置参考汇率」，不会写进缓存冒充网络值。当日缓存直接复用；跨日后先请求，失败时退回旧缓存并标为「历史缓存汇率」。
- **参考表**：来自 CFSM 2.8.5 Stable（`1dc0dc4`）`src/frontend/utils/finance.js` 的 `FINANCE_CURRENCY_CONFIG`，只在没有网络也没有缓存，或备用源缺某个币种时使用，并标为「内置参考汇率」。
- **手动覆盖**：只接受有限正数，CNY 不可改；在汇率页和使用位置都标为「手动」。「恢复今日汇率」只清除手动覆盖；当前表不是当日取得的时，按钮显示为「清除手动汇率」，不承诺网络刷新。
- **存储异常**：localStorage 不可用、配额已满或缓存损坏时，按无缓存处理，页面照常工作。

## 6. 结论与有意差异

审计没有发现需要修改 Worker / Agent / D1、增加后端代理或密钥的地方，全部在前端完成。本次按第 3、5 节实现。与 Komari `bf83765` 的有意差异如下，原因都是 CFSM 数据优先，或者不把「不知道」算成一个数：

| 项 | Komari `bf83765` | 本主题 | 原因 |
|---|---|---|---|
| 未知或空币种 | 按 CNY 计算 | 不换算，逐条注明「未标明币种」「币种无法识别」 | CFSM 用空串表示输入无法识别，当 CNY 会算错 |
| `kr` | 上游没有这个符号；CFSM 前端按 SEK | 标为「币种有歧义」，不换算 | 后台选项的说明本身就包含三种克朗 |
| 缺到期日 | 剩余价值按 0 计入合计 | 不计入，注明「未设置到期时间」 | 缺失不等于已过期 |
| 剩余价值算法 | 剩余天数向上取整，不封顶 | 毫秒比例，封顶为原价（CFSM 口径，第 4 节） | CFSM 数据优先 |
| 免费节点 | 价格 -1 或 `白嫖中` 标签 | 价格 -1 / 0，或 `白嫖中` 标签 | CFSM 的 `isFreePrice` 包含 `0.00` |
| 价格无法识别 | 上游价格是数字，没有这种情况 | 不算免费、不按 0 计入，注明「价格无法识别」；未填价格只在明细行显示「未设置」 | 不把坏数据算成 0 |
| 备用汇率地址 | `api.frankfurter.app`（301 跳转且不带跨域头，浏览器里取不到） | `api.frankfurter.dev` | CFSM 同款地址，线上 CSP 允许 |
| 汇率来源 | 整张表一个状态；缺任一币种整张作废 | 逐币种记录来源；数据源缺的币种用参考表补上并标明 | 参考值不冒充网络值 |
| 手动汇率 | 首页与弹窗生效，详情页不读 | 首页、详情页、弹窗一致，改动后实时生效 | 同一份偏好，所有使用位置结果一致 |
| 明细弹窗布局 | 源码意图是页签在上、内容在下；运行时 `data-horizontal:flex-col` 与 reka 的 `data-orientation` 对不上，页签与内容并排，375 下内容被挤出弹窗 | 按源码意图上下排列 | 不复制运行时缺陷 |
| 按量估算 | 第二个页签，汇总第三栏「当前节点估算」 | 不移植；汇总为两栏，弹窗描述改为「固定账单明细与汇率设置」 | 任务书排除 |
| 合计的说明 | 无 | 合计下方与卡片提示里写出未计入的台数与原因；用到参考、旧缓存或手动汇率时写出来源 | 使用处可辨识 |
| 卡片标记 | 无 | 单位位置显示「载入中」「部分」「参考」「旧汇率」「手动」「不可用」，可以组合，例如「部分 · 参考」 | 同上 |
| 固定费用列 | 币种代码前缀（`USD9.9 / 年`） | CFSM 后台符号（`$36.9 / 年`），与节点卡一致 | 保留原币，沿用 CFSM 写法 |
| 汇率页说明 | 来源 · 更新时间 · 1 CNY 对应数值 | 同一行之外，另写数据源、数据日期与「每日参考汇率，不是实时成交价」；参考补齐与手动覆盖的币种单独列出；当前表不是当日取得时，按钮显示「清除手动汇率」 | 不把日汇率说成实时 |
| 汇率页币种 | 12 种 | 同样 12 种；节点用到的其它币种（CFSM 识别 43 种）追加在后面 | 方便核对与手动覆盖 |
| 详情页未填价格的节点 | —— | 节点价格、月均支出、剩余价值三张卡都不出现。此前月均支出这一张会单独显示「—」 | 三张卡口径一致 |
| 快捷筛选 `monthlyCost` | 预设列出，运行时过滤掉 | 不显示 | 与上游运行结果一致 |

## 7. 实现

| 位置 | 内容 |
|---|---|
| `src/domain/finance.ts` | 共享口径：价格、币种、周期、到期的解析，月均与剩余价值（原币），换算，合计与未计入原因，说明文字 |
| `src/services/exchange-rates.ts` | 两个数据源的请求与解析、超时、缓存读写与校验 |
| `src/stores/finance.ts` | 财务偏好（显示币种、排除免费节点、手动汇率）、汇率状态、共享的进行中请求 |
| `src/domain/theme-presentation.ts` | 首页三张财务卡、详情页剩余价值卡；卡片键与预设按上游顺序 |
| `src/components/finance/FinanceDetailsDialog.vue`、`src/components/ui/AppDialog.vue` | 明细弹窗与通用弹窗外壳（reka Dialog / Tabs） |
| `src/components/dashboard/OverviewCards.vue`、`src/views/HomeView.vue`、`src/views/ServerDetailView.vue` | 入口、权限与请求时机 |
| `src/styles/main.css` | 弹窗样式（「v1.1.7：财务明细弹窗」一段），取值逐项来自上游实测 |
| `src/domain/theme-settings-form.ts` | `generalCardKeys` 与「未登录时隐藏价格」的说明文字 |

浏览器存储（本主题命名空间，不新增 `theme_options` 键）：

| 键 | 内容 | 读写异常时 |
|---|---|---|
| `cfsm-glassmorphism.finance.v1` | `displayCurrency`（默认 CNY）、`excludeFree`（默认开）、`overrides`（只接受有限正数，CNY 不可改） | 逐项校验，无效值回到默认；写入失败时本页照常使用内存中的值 |
| `cfsm-glassmorphism.finance-rates.v1` | 当日汇率：本地日期、取得时间、提供方、数据源日期、数据源实际返回的汇率 | 结构或数值不合格时当作没有缓存 |

显示币种的分工：

| 位置 | 币种 |
|---|---|
| 首页「剩余价值」「月费用估算」「年费用估算」 | 显示币种 |
| 节点卡、列表里的价格 | 原币（未改） |
| 详情页「节点价格」「月均支出」 | 原币 |
| 详情页「剩余价值」 | 显示币种；提示里给出原币金额与汇率来源 |
| 明细弹窗「固定费用」 | 原币与付款周期 |
| 明细弹窗「剩余价值」「月均支出」与合计 | 显示币种 |

权限与隐藏：

| 情形 | 首页 | 明细弹窗 | 详情页 | 汇率请求 |
|---|---|---|---|---|
| 站点关闭 `show_price` | 三张财务卡不渲染 | 没有入口 | 节点价格、月均支出、剩余价值整卡隐藏 | 不请求 |
| 未登录且开启「未登录时隐藏价格」 | 三张卡显示 `***`，不是按钮，提示只有「总价值 ***」 | 打不开 | 沿用既有的整卡隐藏 | 不请求 |
| 详情页冷启动（站点开关还没确认） | —— | —— | 先隐藏，确认可见后才显示 | 确认可见之后才请求 |

## 8. 请求与缓存的影响

**CFSM 后端**：不新增请求。首页与详情页的 `/api` 请求清单不变；本地实测详情页冷启动仍是 `config` / `server` / `history/all` / `servers` 四个。

**第三方汇率服务**：由访客浏览器直接请求，Worker 不参与。

| 情形 | 请求数 |
|---|---|
| 不需要换算：所有计费节点的币种都等于显示币种、价格不可见、没选财务卡，或节点列表加载失败 | 0 |
| 当日已有缓存 | 0 |
| 没有当日缓存，`open.er-api.com` 成功 | 1 |
| 主源失败，`api.frankfurter.dev` 成功 | 2 |
| 两个源都失败 | 2；本页之后不再重试，有旧缓存就用旧缓存，否则用参考表 |

- 触发位置：首页选中的财务卡可见且需要换算；明细弹窗打开且需要换算；详情页价格可见、选了剩余价值卡且节点币种不等于显示币种。
- 同一页面同时触发时共享一个进行中的请求；首页、详情页、弹窗共用一份缓存。
- 单源超时 5 秒；没有轮询，也没有定时刷新。
- 请求地址固定，不含节点名、IP、价格或节点列表；`credentials: 'omit'`，`referrerPolicy: 'no-referrer'`。汇率服务仍能看到访客的 IP 与浏览器标识，这是浏览器直连的固有情况。
- 线上 CSP 的 `connect-src` 已允许这两个地址（第 2 节）。

**对 sr.706632.xyz 的实际影响**（2026-09-17 只读读取）：`generalCardPreset` 为「基础」，`generalCardKeys` 已包含 `remainingValue`，`show_price` 开启，「未登录时隐藏价格」关闭，5 台节点都是美元或欧元计价。应用候选后，首页会出现「剩余价值」卡；每个访客的浏览器每天首次打开首页或详情页时，会向 `open.er-api.com` 发 1 个请求，其后当天复用缓存。

## 9. 验证记录

以下均为**本地**结果：生产构建配合本地 mock（`work/visual-server.mjs`，不入库）。除非特别说明，汇率由 mock 拦截并返回人工测试值 `1 CNY = 0.125 EUR = 0.1 USD`，不代表真实汇率。线上部署尚未验证（候选未应用）。

**单元测试**：
- `tests/finance-domain.test.ts`（27）：币种与符号、免费 / 未设置 / 无效价格、8 个周期、到期、固定样本、换算、参考与手动来源、合计与未计入原因、原始对象不被修改。
- `tests/finance-rates.test.ts`（18）：两个数据源的解析与回退、超时、缓存校验、共享请求、旧缓存、参考表、偏好与存储异常。
- `tests/finance-cards.test.ts`（15）：首页财务卡的上下文、遮蔽、标记与提示，以及入口接线。
- `tests/detail-fidelity.test.ts`：详情页剩余价值换算、各种不可用情形、未填价格。

隐藏尖峰、控件配色与路由的既有回归一并重跑，断言没有放宽。

**浏览器检查**：

| # | 场景 | 结果 |
|---|---|---|
| 1 | 首页 · 网络汇率 | 剩余价值 ¥1,023.46、月费用 ¥87.52、年费用 ¥1,064.79，均标「部分」；1 个请求，`credentials: omit`、`no-referrer`，写入缓存 |
| 2 | 明细弹窗 · 固定账单 | 排除免费后 8 行，数值与卡片一致；NETCUP 剩余价值「—」（未设置到期时间），克朗与空币种「—」并注明原因；合计下方列出未计入原因 |
| 3 | 明细弹窗 · 汇率设置 | 「今日网络汇率 · 取得时间 · 1 CNY 对应数值」与数据源说明；CNY 输入框禁用；数值与测试汇率一致 |
| 4 | 显示币种与手动汇率 | 改为 USD、EUR 手动 0.2 后，卡片与弹窗同时更新，汇率页注明「手动：EUR」，偏好写入本地；「恢复今日汇率」只清除手动值（EUR 回到 0.125） |
| 5 | 排除免费节点 | 取消勾选后多出免费与白嫖两行，剩余价值与月均各 +¥10，卡片同步；恢复勾选后回到原值 |
| 6 | 两个源都失败、没有缓存 | 2 个请求（主源、备用源）；卡片「部分 · 参考」，提示「汇率：内置参考汇率」；汇率页「内置参考汇率 · 无更新时间」，按钮「清除手动汇率」 |
| 7 | 两个源都失败、有旧缓存 | 2 个请求；卡片「部分 · 旧汇率」；弹窗合计下注明「汇率：历史缓存汇率」；汇率页「历史缓存汇率 · 2026/9/1 10:00:00」 |
| 8 | 当日缓存 | 0 个请求；提示「汇率：今日缓存汇率」 |
| 9 | 真实数据源（未拦截） | `open.er-api.com` 1 个请求，200，940ms；缓存 42 种币种；月费用 ¥63.83，与按当日汇率手算一致 |
| 10 | 未登录隐藏价格 | 三张卡 `***`，没有按钮语义，点击与回车都打不开，提示「总价值 ***」，0 个请求；页面中没有价格字符串 |
| 11 | 站点关闭价格 | 首页只剩非财务卡；详情页从冷启动到加载完成，都没有出现节点价格、月均支出、剩余价值或 `€`；0 个请求 |
| 12 | 详情页 | V.PS：节点价格 `€69.98 / 五年`，月均 `€1.15 / 月`，剩余价值 ¥504.94（显示 USD 时为 $50.50，提示「原币 €63.13 / 汇率：今日网络汇率」）；汇率请求发生在 `config` 与 `server` 返回之后 |
| 13 | 键盘 | 回车打开，焦点落在「关闭」；空格打开并阻止页面滚动（浏览器工具发不出真实空格，用合成的按键事件）；Tab / Shift+Tab 在弹窗内循环；方向键切换页签；Esc 关闭后焦点回到卡片，页面指针事件恢复 |
| 14 | 与上游对照截图（1440 / 375 × 浅 / 深 × 两个页签） | 无横向溢出；375 下表格在自己的容器里横向滚动；上游 375 下页签与内容并排，内容被挤出 |
| 15 | 计算样式（浅 / 深） | 弹窗表面、边框、圆角、阴影、模糊，标题栏内边距与分隔线，汇总数值字号，页签条与页签，下拉框与描边按钮，表头与单元格内边距，汇率网格列数与间距，输入框与禁用态，与上游实测逐项相同 |
| 16 | 配色 | 默认、翡翠、自定义方案的浅色与深色下，下拉框与描边按钮取方案的控制色与文字色（自定义：`rgba(255, 0, 0, 0.5)`，文字浅色 `rgb(0, 170, 0)`、深色 `rgb(0, 255, 0)`）；汇率输入框、弹窗表面、页签不随方案；弹窗挂在 `body` 下，变量可达 |
| 17 | 原有功能 | 详情页「隐藏尖峰」在尖峰场景下显示「隐藏尖峰 · 2」并可关闭；「重新加载」随自定义方案；节点列表加载失败时没有财务卡，也没有汇率请求 |

**未覆盖**：
- 线上真实部署，需要用户应用候选后只读复验；
- Safari、Firefox 与真机触屏；
- 节点较多时的表格滚动性能；
- 按量估算，本轮不包含。

## 10. 应用候选后的线上复验清单

只读执行，不修改 `theme_options`、节点账单或后台配置。测试个人浏览器里的财务偏好前，先记下原值，结束后恢复并核对。

1. 首页出现「剩余价值」卡，数值按人民币显示，汇率请求最多 1 个（主源失败时 2 个），不带凭据。
2. 点开明细：5 台节点的固定费用保留原币；NETCUP 的剩余价值显示「—」，合计下方注明未设置到期时间。
3. 汇率页的来源、时间与数据日期如实显示。
4. 详情页：节点价格与月均支出保留原币，剩余价值按人民币显示，提示里有原币金额。
5. 「隐藏尖峰」与「重新加载」的配色照旧，375 与深色下无横向溢出，控制台无报错。

2026-09-17 已按此清单复验候选产物 `73db7fe5`，结果全部符合，逐项记录与线上没有触发的场景见 `docs/todo.md` TODO-08。
