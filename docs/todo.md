# 正式版待办

## 部署记录

| 日期 | 站点 | 已应用产物 | 对应源码 | 核验 |
|---|---|---|---|---|
| 2026-09-12 | https://sr.706632.xyz | `theme-v1.1.1` = `81ecb3884f7f2c6e3888680a9e29a38c90c246c9` | `v1.1.1` = `e26af7f` | 线上 `index-D8tgaFpd.js` / `index-JhAy0QYE.css` 与发布 ZIP 内容逐字节相同；入口引用的 5 个懒加载分块全部 200；线上冒烟通过（首页、详情页、链接规范化、旧链接兼容、上一个节点、无控制台错误） |
| 2026-09-13 | https://sr.706632.xyz | `theme-v1.1.2` = `a9b23501dc7c1a685dd5d56ee3dd59756349bb0c`（验收时为候选产物 `30df8b88`） | `v1.1.2` = `a3800bb` | 线上 `index-CDg-FS4G.js` 与 `index-C_NwXGvk.css` 的 sha256 与候选逐字节相同，22 个资源全部 200；index.html 的差异仅为 CFSM 注入的站点标题与 favicon。顶部专项验收见 TODO-07 |
| 2026-09-15 | https://sr.706632.xyz | `theme-v1.1.3` = `63eaddc97bd1656d514ea20109b86abc6d6db73a` | `v1.1.3` = `35b7a283` | 线上 `index-aI-afTXR.js` 与 `index-PutMzEX9.css` 与发布产物逐字节相同，22 个资源全部 200；冷启动 4 个请求（`config` / `server?id=` / `history/all?hours=1` / `servers`），此前为 6 个；两图默认同为 1 小时，切到 1 天时联动且只新增 1 个请求；详情页链接无 `source=`，28 张图卡 / 8 个 canvas 正常着色 |
| 2026-09-15 | https://sr.706632.xyz | `theme-v1.1.4` = `bd9b0d413655880f30b35422ee03516c5b0a8a95`（验收时为候选产物 `bdc2214b`） | `v1.1.4` = `5b79583` | 候选 22 个资源全部 200 且与本地构建逐字节相同，`index.html` 的差异仅为 CFSM 注入的站点标题与图标、以及 Cloudflare 自身的脚本；详情页冷启动 4 个请求，从首页进入 2 个；负载图切换 1 个且两图联动，延迟图选同窗口 0 个、选不同窗口 1 个；延迟图提示已不存在，两行控件在 1440 浅色 / 1440 深色 / 375 深色下同为 `flex: 0 0 auto`、居中、8px、16px，无横向溢出，全程无控制台报错。候选源码 `a2cda8a` 与发布源码 `5b79583` 的 `src/` 差异为空 |
| 2026-09-15 | https://sr.706632.xyz | `theme-v1.1.5` = `a519413fce4280974170f62d1d4847cdead767a3`（**尚未应用**：站点仍固定在 `bd9b0d41` = v1.1.4） | `v1.1.5` = `4c83a77` | 发布产物自洽：包内 24 个文件，`index.html` 引用的两个入口在 raw 上均 200；设置页分块 `ThemeSettingsView-B7Bwd9KD.js` 命中新文案、旧文案 0 处。站点应用后需另行验收 |
| 2026-09-15 | https://sr.706632.xyz | `theme-v1.1.6` = `55ea0553509197139f69fca7ac1618387086090e`（验收时为候选产物 `487dbf1f`，站点当前跑的就是该候选） | `v1.1.6` = `b2d6047` | 候选 23 个资源全部 200 且与本地构建逐字节相同；顶栏实测渲染站点图标（原生 256×256、按 30×30 裁进圆形容器，容器伪元素 content 为 none）；设置页四处更正在线，取值 5 且下限 5；详情页冷启动仍 4 个请求，延迟图无提示，两图默认 1 小时；深色与 375 下均无横向溢出、无控制台报错。候选源码 `9288390` 与发布源码 `b2d6047` 的 `src/` 差异为空 |
| 2026-09-17 | https://sr.706632.xyz | 候选产物 `73db7fe5fb644c2ab6ac8be8ecdf881ba19f1c93`（`candidate-3e55557`；v1.1.7 尚未发布） | `3e55557` | 24 个资源全部 200 且与候选逐字节相同，`index.html` 的差异仅为 CFSM 注入的站点标题与图标、以及 Cloudflare 自身的脚本；首页、明细弹窗、详情页、375 深色的只读复验见 TODO-08 |

历史记录：TODO-05 的验收对应候选 `2c4ab2c`，TODO-06 的回归对应候选 `fbe35bd`。

五个 `candidate-*` 分支已在 v1.1.2 发布后删除——正式版的不可变标签就是回退入口，候选分支不再承担这个角色。产物 commit 存档如下：

| 分支（已删除） | 产物 commit | 对应源码 |
|---|---|---|
| `candidate-a088ed4` | `2c4ab2c89e3e71f82ddad3430596dea87f418863` | `a088ed4` |
| `candidate-3baf452` | `fbe35bdb8b554cf9e407ccabdfee5beb99765f82` | `3baf452` |
| `candidate-c15da63` | `3bae821603af1dd2ea90fed089f0eacf84d491ec` | `c15da63`（未应用） |
| `candidate-6b1b4f2` | `b186db6b8abe68903abd34ef361dd7cdb360bb44` | `6b1b4f2`（未应用） |
| `candidate-3fb26c4` | `30df8b8833871d733f412b00e8f5eab032c47326` | `3fb26c4`（v1.1.2 的验收对象） |

v1.1.3 的候选分支 `candidate-40939f5`（产物 `af0d0a25f2e857b961050ccf35b974f4bc389d15`，源码 `40939f5`）在发布后同样删除；候选与发布之间只有 README 与版本号的差异，业务代码零变化。

v1.1.4 的候选分支 `candidate-a2cda8a`（产物 `bdc2214b528fb96229c0e0ff5c1fce98f528929c`，源码 `a2cda8a`）是用户线上验收的对象，发布后删除；发布产物的 CSS 与候选同名同哈希，JS 分块哈希不同，源码 `src/` 差异为空。

v1.1.6 的候选分支 `candidate-9288390`（产物 `487dbf1f2e166f509f722d7c987837d98c644240`，源码 `9288390`）是用户线上验收的对象，发布后删除；发布产物的 CSS 与候选同名同哈希，JS 分块哈希不同，源码 `src/` 差异为空。v1.1.5 未走候选流程，直接发布。

回退入口：`theme-v1.1.6` / `theme-v1.1.5` / `theme-v1.1.4` / `theme-v1.1.3` / `theme-v1.1.2` / `theme-v1.1.1` / `theme-v1.1.0` 等版本标签，均为不可变。发布包与候选的字节差异来源见 `docs/releases/v1.1.1.md` 之外的说明：源码 commit 与 bun 版本相同，差异仅为 Linux CI 与 Windows 本地构建的分块哈希不可跨平台复现，逻辑分块集合一致，并已用发布字节重新冒烟。

> 本文件只记录**正式版（稳定版）发布前需要处理**的事项，每条写明现状、上游做法与待决策点。
>
> 已确认存在但不阻塞的数据 / 逻辑问题在 `docs/known-bugs.md`；逐项保真差异在 `docs/fidelity-audit.md`；
> 单轮次的缺陷矩阵在 `docs/bug-matrix.md`。

## TODO-01 · 延迟图对极高值的处理（v1.1.7 候选已实现「隐藏尖峰」，尚未发布）

- **v1.1.7 候选的做法**：用户批准在「曲线平滑」后面增加独立的「隐藏尖峰」开关。这是本主题自己的绘图扩展，不是上游移植。两个开关互不排斥，默认都关，共用一个「图表显示说明」入口（原先只解释曲线平滑的提示已删除）。开关状态只在当前图表组件里保留，不写入浏览器偏好，也不新增后台字段。
  - **检测**（`src/domain/spike-mask.ts`）：每条线路单独判断，只命中「两侧都有充分证据的单个孤立高点」。阈值集中在 `SPIKE_PARAMS`：
    - 每侧最多取 4 个、至少要有 2 个有效邻居；遇到原始缺失，或相邻样本间隔超过「中位采样间隔 × 2」，就停止取邻居；
    - 基线取两侧邻居的中位数，噪声取 MAD × 1.4826，且不低于 max(2ms, 基线 × 5%)；
    - 候选点要同时满足：比基线高至少 100ms，至少是基线的 2 倍，高出量至少是噪声的 6 倍；
    - 紧邻的左右两个样本本身不能也算高；左右两侧中位数之差不能超过 max(100ms, 基线) × 0.5；
    - 如果一条原本有数据的线会被整条遮蔽，整条回退为完整显示。
  - **绘图**：命中点只在绘图副本里置为 null 占位，横轴不错位，`connectNulls: false` 不变，隐藏处留断口。纵轴按仍可见的数据自动收缩，所有线路共用一个纵轴。开关开启时按钮显示「隐藏尖峰 · N」，N 只数当前选中且图例可见的线路上被遮蔽的点。
  - **不变的部分**：Pinia 状态、接口响应和任务卡统计（平均、P50 / P99、波动、丢包）都从原始数据计算，四种开关组合下逐项相等；首页 Ping / Loss 的口径不动；切换开关不发请求。悬浮到被隐藏的时刻，提示框显示真实数值并标注「已隐藏」。
- **仍然存在的限制**（如实保留，发布说明要写明）：
  - 只隐藏单个孤立高点。连续两个及以上的高点、持续高延迟、阶跃抬升、缓慢爬升一律保留，它们仍会决定纵轴上界；
  - 序列首尾的高点，以及紧挨缺失或时间缺口的高点，证据不足，保留；
  - 抖动大的线路上，落在噪声范围内的高值不算尖峰；
  - 「曲线平滑」在真实阶跃处仍会画出圆角和轻微下探，这是既有 `smooth: 0.6` 的行为，本轮没有改；
  - 说明气泡的触屏行为只在桌面浏览器里用模拟触摸事件验证过，没有真机证据。
- **上游做法**（不移植）：`cutPeakValues` 先把与邻域均值（窗口 15）偏离超过 30% 的点置空，再用 EWMA（α=0.3）重写整条序列，并用运行值填补空洞。而且无论开关状态，上游都会先跑 `interpolateNullsLinear`：相邻有效点间隔不超过中位数 × 6 时线性插值，间隔钳制在 2–30 分钟。上游画出的线已经不是采样值本身，与本主题「不改写采样、不插值」的边界冲突。
- **此前的待决策**：原先列过三个方向：A 保持现状；B 原样移植上游削峰；C 只改坐标轴（按 P99 缩放或对数轴）。v1.1.7 采用的是第四种：数据不动，只在绘图副本里遮蔽孤立高点。它不等于 B，也没有引入对数轴或按分位数截断坐标轴。开关的旧名「平滑峰值」已在 `837864b` 改为「曲线平滑」。
- **影响文件**：`src/domain/spike-mask.ts`、`src/domain/detail-chart-options.ts`、`src/components/detail/PingChart.vue`；测试见 `tests/spike-mask.test.ts`、`tests/ping-spike-chart.test.ts`。验证记录见 `docs/chart-parity.md` 的「v1.1.7」一节。

## TODO-02 · CFSM 不支持的两项设置的最终处理

- **现状**：`rpcTransportMode` 与 `visitorInfoEnabled` 已从设置页移除；两项仍保留在 `theme/settings.ts` 的 48 项 schema 与 `POST /api/theme_options` 的完整快照中，因为保存协议要求发送完整对象。
- **复核依据**：CFSM 公开接口只有 REST 与 `/api/ws`，后端源码与 `theme-develop.md` 中都不存在 RPC 传输层；在 CFSM 整个 `src/` 中搜索 `CF-Connecting-IP` / `visitor` / `访客` 零命中，没有任何接口把访客 IP 或审计数据交给主题。
- **待决策**：正式版是否把这两项一并从 schema 中移除。移除会改变保存快照的键集合，需要先确认与旧版本互相覆盖时的兼容行为（现有「未知后端键原样回传」的规则是否足够）。

## TODO-03 · 配色预设的表面色（v1.1.2 完成，并更正原结论）

上游把预设的 16 个值写到 `:root`，样式表里写了三组选择器。**"写了规则"不等于"有元素消费"**——
v1.1.2 在同一台机器、同一浏览器里跑固定版本的 Komari（`bf83765`）逐条实测后，前两行的原结论被推翻：

| 上游选择器 | 消费的变量 | 上游实际命中的元素 | 本主题对应 | 结论 |
|---|---|---|---|---|
| `.node-card, .bg-card, [data-slot='card']` | card / hover / border / shadow | 节点卡（`[data-slot='card']` 在上游命中 0 个，上游没有这个属性） | `.node-card` | **已对齐**（v1.1.1）：取值逐字来自上游 `PRESET_TOKENS` |
| `header` | header | **0 个**。上游 `src/` 内没有任何 `<header>` 元素，运行时 `document.querySelectorAll('header').length === 0`；顶栏主体是 `div` | `.app-header` | **原结论更正**：不存在"上游顶栏常驻预设表面色"这回事，该规则是死代码，`--glass-*-header` 无人消费 |
| `.bg-background`（注释写的是"顶部统计栏"） | control | 顶部统计卡**不命中**——它们的类是 `bg-background/50` 与 `hover:bg-background`，类名不同；CardX 的 tailwind-merge 还会把默认的 `bg-card` 合并掉。真正命中的是选中态控制胶囊、对话框输入框、outline 按钮、Alert，首页默认状态下一个都不渲染 | 本主题无一一对应元素（后续更正：激活态快捷筛选胶囊与加载失败时的「重新加载」有对应，分别在 v1.1.3、v1.1.7 接入，见下方「v1.1.7」一节） | **原结论更正**：该注释有误导，预设与顶部统计卡无关 |

### 实测取值（1440×900，浅色，五卡无地球；深色两侧取值相同）

| 元素 / 状态 | Komari `bf83765` | 本主题 v1.1.1 | 本主题 v1.1.2 | 结论 |
|---|---|---|---|---|
| 顶栏 · 页面顶部 | transparent / none / 1px transparent / none | 同左 | 同左 | 本来就一致 |
| 顶栏 · 滚动后 | background `rgba(0,0,0,0)`、`blur(16px)`、border `oklab(0.554 -0.0100213 -0.0448951 / 0.1)`、shadow `none` | background `color(srgb .976 .984 .992 / .7744)`、`blur(20px) saturate(1.55)`、border `rgba(78,102,130,.13)`、shadow `rgba(20,40,65,.07) 0 8px 28px` | 与上游逐项相同（`#62748e1a` 即 slate-500/10） | **已修复** |
| 顶栏 · 回到顶部 | 同"页面顶部" | 同左 | 同左 | 本来就一致 |
| 顶栏几何 | 57px；内容宽 1280 @ x=75 | 同左 | 同左 | 未改动 |
| 统计卡 · 常态 | `oklab(0.935 -0.00463525 -0.0142658 / 0.5)`、backdrop none（<768px 为 `blur(8px)`）、border `0px none`、shadow `none`、radius 10px | 逐项相同 | 逐项相同 | 本来就一致 |
| 统计卡 · 真实悬浮 | `oklch(0.935 0.015 252)`（不透明 `--background`） | 相同 | 相同 | 本来就一致 |
| 统计卡 · 图标 | 常态 slate-500/20、悬浮 slate-500 | 常态 `--muted` 30%、悬浮 `--muted` | 按上游原样移植为 `#62748e33` / `#62748e` | **已修复** |
| 统计卡 · 深色 | `oklab(0.141 0.00136333 -0.00481054 / 0.5)`，文字 `oklch(0.985 0 0)` / `oklch(0.86 0.012 286.067)` | 逐项相同 | 逐项相同 | 本来就一致 |

### 预设隔离复测（v1.1.2）

翡翠 / 柔和 / 高对比 / 午夜 / 自定义五种配置逐一重新加载后实测：顶栏三态与统计卡表面色**不随预设变化**（与上游一致），
`--node-card-surface` 按预设变化（自定义 `#ff000080` → 节点卡 `rgba(255,0,0,0.5)`），
`--glass-strong` 始终没有被写成行内样式，提示框 / 菜单 / 弹窗 / 设置面板不受预设污染。

- **自定义配色不变**：仍是原来的 10 个键，hover / header / shadow 按上游同样的派生规则补齐，用户不需要多填字段。
- **未对齐（有意保留）**：本主题的控制胶囊、分段选择器等没有接到 `control` 令牌。上游那些元素确实消费该令牌，
  但它们不在顶部区域，v1.1.2 是顶部专项，不顺带改动；此处记为待评估，不作为已解决。
  （后续：v1.1.3 接入了激活态快捷筛选胶囊；v1.1.7 逐项核对了其余消费者，见下方「v1.1.7」一节。）

### v1.1.7：控制色的消费者逐项核对（候选，尚未发布）

v1.1.2 至 v1.1.6 的发布说明在「已知限制」里写过「对话框输入框与描边按钮不随配色方案变化」。v1.1.7 在固定版本的上游里逐个打开相关界面，按真实 DOM 核对：哪些元素真的带 `bg-background` 这个类，本主题有没有对应元素。上游的本地 mock 为此加了两个开关：`?me=1` 模拟登录，用来打开只对管理员显示的首页工具；`?nodesfail=1` 让节点列表请求失败。两个开关都在 `work/` 里，只影响本地参照环境。

**上游规则**（构建产物原文）：`.bg-background{background-color:var(--glass-light-control,#f1f5f9c2)!important;-webkit-backdrop-filter:blur(14px)saturate(145%)!important}`，深色版本只换控制色变量。文字色 `var(--glass-*-text)!important`，内部的 `.text-muted-foreground` 取 `--glass-*-muted-text`。边框不在规则里。

**上游真正的消费者**（运行时带 `bg-background` 类）：

| 上游元素 | 出现条件 | 本主题对应 | 结论 |
|---|---|---|---|
| 激活态快捷筛选胶囊（`HomeView`） | 点选任一快捷筛选 | `.quick-controls button.is-active` | v1.1.3 已接入；本轮五种方案 × 明暗复测，逐套跟随 |
| 连接失败提示里的描边「重试」（`App.vue`） | 首次拉取节点列表失败（`connectionError = !nodesAvailable`） | 首页「无法加载节点」与详情页加载失败面板里的「重新加载」 | **v1.1.7 接入**，见下方实测 |
| 财务明细对话框的 6 个下拉框与 2 个描边按钮（「带入当前 uptime」「恢复今日汇率」） | 点开「剩余价值」等财务卡 | 财务追加轮移植的明细弹窗：固定账单页与汇率页的「显示币种」下拉框、汇率页的「恢复今日汇率」描边按钮 | **v1.1.7 财务追加接入**；「按量估算」页签（4 个下拉框与「带入当前 uptime」）没有移植，见 `docs/finance-parity.md` |
| 负载图 / 延迟图自定义时间范围的「应用」 | 选择自定义范围（实测：禁用时底色不变、整体透明度 0.5；旁边的时间输入框是 `bg-background/50`，不消费） | 无，CFSM 只接受固定时段 | 不适用 |
| 健康工具的选中时间范围（日 / 周 / 月 / 有史以来） | 登录后打开「健康」 | 无，本主题的健康摘要只用 `/api/servers` 的窗口，没有范围按钮 | 不适用 |
| 拓扑工具的选中视图（ASN / 标签 / 根因） | 登录后打开「拓扑」 | 无，本主题的「拓扑」是地区与分组的分类，没有视图切换 | 不适用 |
| 节点对比的已选节点胶囊 | 登录后打开「对比」并选中节点 | 无，本主题没有节点对比 | 不适用 |

连接失败提示本身是 `destructive` 变体，它的 `bg-background` 被 `bg-destructive/10` 合并掉了，提示条不跟随方案，只有里面的按钮跟随。

**本主题有对应元素、但上游对应元素并不消费方案的控件**（类名不同，`.bg-background` 选择器匹配不到）：

| 本主题元素 | 上游对应 | 上游实际类名 | 本主题实测 |
|---|---|---|---|
| 首页搜索框 `.search-field input` | 首页搜索 `Input` | `bg-transparent` | 五种方案 × 明暗下完全不变 |
| 视图切换 `.view-switch button` | 卡片 / 列表切换（outline） | `bg-background/50`，激活时加 `!bg-background` | 同上 |
| 高级工具开关 `.tool-switch button` | 首页工具按钮 | 激活时为 `!bg-background` | 同上 |
| 快照导出的二级确认密码框 `.snapshot-password input` | `SnapshotExportPanel` 的 `Input` | `bg-background/60` | 同上 |
| 「导出 JSON / CSV」`.snapshot-actions button` | 同面板的描边按钮 | `bg-background/60`（tailwind-merge 合并掉了 outline 自带的 `bg-background`） | 同上 |
| 详情页节点选择器 `.detail-topbar__select` | `InstanceDetail` 的 `<select>` | `bg-transparent` | 同上；底色透明，文字色与上游相同 |

「完全不变」的依据：首页八个控件的计算样式（底色 / 文字 / 边框 / 模糊）拼成签名，翡翠、柔和、高对比、午夜、自定义五套逐一保存并重新加载后比较，浅色五套签名相同，深色五套签名相同；同一轮里激活态胶囊与节点卡逐套变化。详情页与设置页用翡翠与自定义两套、浅深两种对照，逐项相同。

**上游没有对应物的本主题控件**，不做语义映射：

| 本主题元素 | 原因 |
|---|---|
| 详情页加载失败面板里的「返回首页」 | 上游对应的是 `Empty` 里的实心主按钮，不带 `bg-background` |
| 图表加载失败的「重试」 | 上游图表没有重试按钮 |
| 主题设置页的输入框、下拉框、文本域与操作按钮 | 上游主题没有设置页，配置由 Komari 后台渲染；这些控件继续用主题自身的表面色 |

这些位置都没有危险操作按钮。加载失败面板的红色边框保留原样，继续承担警示。

**v1.1.7 接入「重新加载」的实测**（本地生产构建，无头 Chrome，1440 与 375）：

| 方案 · 明暗 | 上游「重试」 | 本主题修改前 | 本主题修改后 |
|---|---|---|---|
| 翡翠 · 浅色 | `rgba(226, 232, 240, 0.76)` / `rgb(16, 21, 28)` | `rgba(249, 251, 253, 0.88)` / `oklch(0.11 0.01 285.823)` | `rgba(226, 232, 240, 0.76)` / `rgb(16, 21, 28)` |
| 翡翠 · 深色 | `rgba(16, 22, 36, 0.85)` / `rgb(248, 250, 252)` | `rgba(13, 20, 32, 0.91)` / `oklch(0.985 0 0)` | `rgba(16, 22, 36, 0.85)` / `rgb(248, 250, 252)` |
| 自定义（控制色 `#ff000080`） | 变量替换后 `rgba(255, 0, 0, 0.5)`，文字随方案 | 不变 | `rgba(255, 0, 0, 0.5)`，文字浅色 `rgb(0, 170, 0)`、深色 `rgb(0, 255, 0)` |

- 只接底色与文字色，这是上游随方案变化的两项；上游规则带 `!important`，悬浮时这两项也不变，本主题这两个按钮本来就没有悬浮规则。
- 边框（上游浅色 `oklch(0.92 0.004 286.32)`、深色 `oklch(1 0 0 / 0.15)`）、8px 圆角、32px 高度、`shadow-xs` 都不随方案变化，本主题保持原有的边框、圆角与尺寸，不在本轮对齐。
- 同一面板里的「返回首页」修改前后不变；375 下没有横向溢出。
- `tests/preset-control-consumers.test.ts` 把 `--control-surface` 的消费者钉在这两条规则上，并禁止给全站输入框或按钮加通配覆盖。

**财务弹窗的上游取值**（`FinanceDetailsDialog`，浅色，默认 mock 方案；财务追加轮已按此接入，实测见 `docs/finance-parity.md` 第 9 节）：下拉框底色 `rgba(226, 232, 240, 0.76)`、文字 `rgb(16, 21, 28)`、边框 `1px solid oklch(0.92 0.004 286.32)`（`border-input`，不随方案）、圆角 8px，估算表单的下拉框高 36px（`h-9`），币种选择与描边按钮高 32px。把 `--glass-light-control` 换成 `#ff000080` 后底色变为 `rgba(255, 0, 0, 0.5)`，把 `--glass-light-text` 换成 `#00ff00` 后文字变绿，边框不变。深色走同一条规则，取值与上表的「重试」相同。

**关于模糊**：上游构建产物里有 21 条声明了 `backdrop-filter` 的规则（含关闭模糊的覆盖），16 条两种写法都有，5 条只剩 `-webkit-backdrop-filter`，恰好是配色方案的几条：卡片、`header`、`.bg-background`，以及 Firefox / 旧 WebKit 下把它们关掉的两条覆盖。Chrome 152 不认前缀写法，所以上游这些表面在 Chromium 里实测都没有模糊，只有 Safari 有。本主题按源码意图保留模糊（节点卡、激活态胶囊、「重新加载」都是 `blur(14px) saturate(145%)`），并且每条都双写；`tests/backdrop-filter-pairs.test.ts` 要求每条模糊规则两种写法都有、取值一致。

**结论**：「对话框输入框与描边按钮不随配色方案变化」这条已知限制不再成立，应更正为：上游消费控制色的元素中，本主题有对应物的几处（激活态快捷筛选胶囊、加载失败时的「重新加载」、财务明细弹窗的下拉框与描边按钮）都已跟随配色方案；其余消费者本主题没有对应界面。财务弹窗是同一个 v1.1.7 候选里的财务追加，接入方式与实测见下方 TODO-08。

## TODO-04 · 节点卡指标盒的内边距（记录更正，源码原本就是对的）

- **更正一**：这条待办最初写的"上游 compact 6px、mini 4px，本主题统一 8px"是错的。源码里 `.node-card--mini .node-box { padding: 4px }` 与 `.node-card--compact .node-box { padding: 6px }` 在 v1.1.1-test.3 时就都已存在，基础档 `6px 8px` 对应上游舒适 / 宽松的 `px-2 py-1.5`，四档都与上游一致。
- **更正二**：`837864b` 依据上面那条错误记录，又插入了一段与既有规则完全相同的 compact 声明。重复声明不改变生效值，也不能作为"本次释放 4px"的证据；重复已清理。
- **教训**：那次判断来自一段被 `head` 截断的 `grep` 输出，截断位置正好在 mini 规则末尾。读取被截断时不得据此下结论。
- **仍需**：源码声明正确不等于渲染结果已验收——四档指标盒的真实 padding 与文字截断情况并入 TODO-05，在浏览器里实测。

## TODO-05 · 舒适与宽松两档的真实环境复测（已完成，对应旧候选）

> **适用范围**：以下结论只对应候选产物 `2c4ab2c`（源码 `a088ed4`）。TODO-06 修复后另有新候选，其回归结论单独记录，不得把本节直接当作新候选的验收结果。

- **验收对象**：产物 commit `2c4ab2c89e3e71f82ddad3430596dea87f418863`（源码 `a088ed4`），由用户应用到 https://sr.706632.xyz 。应用结果用字节核验：线上 `/assets/index-C9vcfAPd.js` 与 `/assets/index-JhAy0QYE.css` 的 sha256 与候选产物逐字节相同。
- **站点环境**：Worker `2.8.5 Beta5`、公开站、5 台真实节点（DMIT / V.PS / 绿云 / NETCUP / Raksmart）、`theme_options` = 翡翠 / compact / 跟随系统、延迟窗口 20 点 2 小时、长历史 180 点。尺寸与预设切换只用浏览器本地覆盖，验收后已清除，未改动生产配置。
- **四档几何（真实数据）**：

| 档 | 1440 | 375 | 头部 | 延迟面板 | 其它 |
|---|---|---|---|---|---|
| 迷你 | 303×206（4 列） | 343×206（单列） | — | 28 | — |
| 紧凑 | 303×300（4 列） | 343×300（单列） | 44 | 44 | 指标盒 `padding: 6px` |
| 舒适 | 405×304（3 列） | 343×304（单列） | 44 | 48 | — |
| 宽松 | 614×340（2 列） | 343×340（单列） | 52 | 56 | 主体内边距 `0 24px 24px` |

  每一档内 5 张卡片高度完全一致；四档 × 两个宽度**全部无横向溢出**；名称、芯片、盒内文字与标签的 `scrollWidth > clientWidth` 检测**全部为 0**，真实数据下没有截断。
- **TODO-04 的浏览器实测**：compact 指标盒实测 `padding: 6px`，与上游一致，真实数据下无截断——源码原本就是对的这一结论得到渲染层确认。
- **预设与明暗**：高对比 + 深色下节点卡 `#020617f2` → `rgba(2,6,23,0.95)`、边框 0.24、文字纯白；全局 `--glass` 仍是 `:root` 深色默认 `rgb(11 17 27 / 76%)`，未被预设覆盖。清除本地覆盖并真实重载后回到生产的翡翠 `#f1f5f9bd`。
- **面板与浮层**：`.glass-panel` 用 `linear-gradient(145deg, var(--glass-strong), var(--glass))`，线上深色解析为 `rgba(13,20,32,0.91) → rgba(11,17,27,0.76)`，即 `:root` 默认值；真实悬浮出的 tooltip `.app-tooltip__bubble` 96×32、背景 `rgba(13,20,32,0.91)`、文字 `oklch(0.985 0 0)`，对比充足，没有透明度异常。
- **设置页**：真实部署上 8 组 **46 个控件**，无横向溢出。
- **详情页**：不带 `?source=` 直接访问节点详情正常（7 张负载图卡、8 个 canvas、延迟区三条真实线路），无错误框、无溢出。
- **控制台**：重载后的首页与设置页读取时均无 error 级日志。平台会在导航时清空控制台历史，因此这不是"整段会话零错误"的结论。
- **未覆盖的场景（缺口，如实记录）**：线上这 5 台节点没有覆盖超长节点名、超长系统名、无价格 / 免费、已过期、无探测目标、离线节点与稀疏字段。这些只能等真实出现，或用本地构造样本补测，不得声称真实环境已覆盖。

## TODO-06 · 详情页 URL 里的 `?source=`

- **现象**：从首页点卡片进入详情时，URL 带上 `?source=https://<站点域名>`。
- **来源**：本主题自己加的参数，写入点在 `views/HomeView.vue` 的 `openServer` 与 `views/ServerDetailView.vue` 的上一台 / 下一台；用于多 `apiBase` 部署时把详情与历史请求送回拥有该节点的后端（source ownership）。读取处只在该值属于已配置 base 时才采用。
- **事实**：单后端站点上完全冗余。线上实测**不带**该参数直接访问详情页，节点、负载图与延迟区都正常。
- **处理（已完成）**：详情页链接改由 `src/router/links.ts` 的 `serverDetailLocation` 统一生成，首页点击与详情页上一个 / 下一个节点都走它；只有 `hasMultipleSources(app.apiBases)` 为真时才写入 `source`。读取端在单后端时直接忽略该参数，因此剥离它不会让 `watch([routeId, requestedSource])` 重复触发加载；多来源部署的归属信息一字未动。
- **旧链接兼容**：带 `?source=` 的地址照旧能打开，进入后用 `router.replace` 抹掉该参数并保留其余查询参数，不额外增加返回记录。
- **本地回归（1440，模拟数据）**：首页点击 → `#/server/<id>` 无 source、历史 +1；上一个 / 下一个连续切换 04 → 05 → 04 正确且无 source；旧链接 `?source=…&keep=1` → `#/server/…?keep=1`（source 去除、keep 保留、历史 +1）；前进 / 后退正常；节点名与该 ID 的接口数据一致。
- **真实环境回归（新候选，独立于 TODO-05 的旧候选结论）**：验收对象为产物 `fbe35bdb8b554cf9e407ccabdfee5beb99765f82`（源码 `3baf452`），线上 `assets/index-CbDFiT57.js` 与 `assets/index-JhAy0QYE.css` 的 sha256 与候选逐字节相同。1440 实测五台真实节点：

  | 路径 | 结果 |
  |---|---|
  | 首页点击进入详情 | `#/server/0ad0c79f…`（DMIT），无 source，历史 +1，7 张负载图卡；请求基线 1× `/api/server` + 1× `/api/config` + 2× `/api/history/all` |
  | 上一个 / 下一个连续切换 | DMIT → V.PS → 绿云 →（上一个）V.PS，顺序正确，全程无 source，每页 7 卡 / 8 canvas |
  | 无 source 深链接直开 | 绿云，UI 名称与 `/api/server?id=` 返回一致，7 卡 / 8 canvas、三条真实线路（电信 96ms·1.37% / 联通 98ms·0% / 移动 84ms·2.74%），无错误框、无溢出 |
  | 同一深链接刷新 | 结果一致，无 source |
  | 旧带 source 链接兼容 | `?source=…&keep=1` → `?keep=1`；source 去除、keep 保留、**历史仅 +1**；请求清单与基线**完全相同**（1 server + 1 config + 2 history），没有重复加载 |
  | 前进 / 后退 | 后退回上一节点、前进回到规范化后的地址，均无 source |

  控制台：读取时无 error 级日志（平台在导航时清空历史，不作为整段会话零错误的结论）。
- **未覆盖**：线上只有单后端，多 `apiBase` 下 `source` 必须保留这条分支只有单元测试与本地覆盖，没有真实环境证据。

## TODO-07 · 顶部统计卡的数值行结构（v1.1.2 完成）

- **现象**：单位与"已用 / 总量"说明相对主数值上浮，且与主数值贴死没有间距。
- **根因（实测，不是推断）**：`OverviewCards.vue` 把 `overview-card__value` 作为**透传属性**交给 `AppTooltip`，
  而 `AppTooltip` 的根节点是只渲染插槽的 `TooltipProvider`（片段根），Vue 无法把透传属性落到任何元素上。
  浏览器实测该类在页面里命中 **0 个元素**，数值行退回 `.app-tooltip` 的 `inline-flex`
  （`align-items` 为 stretch、无 `gap`），单位盒被拉伸到 24px。
- **实测差值（1440×900，浅色）**：修复前单位基线比主数值高 **9px**、间距 **0**；
  修复后基线差 **0**、间距 **4px**，与 Komari `bf83765` 同位取值逐项相同。
  375 / 767 / 768 / 1440 / 1920 与深色同样复测通过。
- **修复**：数值行改为 Tooltip **内部**独立的 `div.overview-card__value`（`display:flex; align-items:baseline; gap:4px`）；
  `AppTooltip` 改为 `inheritAttrs: false` + 触发器 `v-bind="$attrs"`，让调用点的类真正落到触发元素；
  包裹层 `.overview-card__value-slot` 为布局中性的 `display:block`（对应上游 DataTooltip 的 `inline-block min-w-0`）。
  单位与标题补上上游的行高（`text-[11px]` → 1.5 即 16.5px；`md:text-xs` 与 `text-xs` → 16px）。
  没有使用 `translateY`、负边距、写死宽度或逐卡补丁。
- **测试**：`tests/overview-value-row.test.ts`（SSR 渲染真实 DOM）。把 `AppTooltip` 的透传修复单独回退后，7 项里有 4 项立即失败，恢复后全通过。
- **真实环境验收（2026-09-13，候选 `30df8b88`）**：线上就是用户的"无地球、五张统计卡"场景（内存 / 硬盘 / 累计流量 / 实时上行 / 实时下行）。

  | 检查项 | 结果 |
  |---|---|
  | 数值行结构 | 5 张卡全部有 `.overview-card__value` 与 `.overview-card__value-slot`（包裹层 `display: block`） |
  | 基线与间距 | 5 张卡在 1440 明 / 1440 暗 / 375 暗 下均为 **基线差 0、间距 4px**；单位 `line-height` 桌面 16px、移动 16.5px，单位盒不再被拉伸 |
  | 统计卡表面 | `oklab(0.935 -0.00463525 -0.0142658 / 0.5)`（暗色 `oklab(0.141 0.00136333 -0.00481054 / 0.5)`）、backdrop 桌面 none / 375 为 `blur(8px)`、border `0px none`、shadow `none`、radius 10px |
  | 真实悬浮 / 移出 | 背景 `oklch(0.935 0.015 252)` ↔ 半透明来回正确；图标 `rgb(98,116,142)` ↔ `rgba(98,116,142,0.2)` |
  | 顶栏三态 | 顶部与回顶 `transparent / none / 透明边框 / 无阴影`；滚动后 `transparent / blur(16px) / rgba(98,116,142,0.1) / none`。首页、详情页、设置页一致，明暗取值相同 |
  | 顶栏几何 | 57px，内容 1280×56 |
  | 完整值入口 | 悬浮数值行弹出气泡，内容 `4.4 GB / 13.9 GB + 31.8%`；气泡底色 `rgba(249,251,253,0.88)`，未被预设污染 |
  | 375 截断 | 内存与硬盘两张卡主数值与单位都截断（6 列栅格下卡宽 109px），完整值由气泡承担 |
  | 附带回归 | 详情页链接无 `source=`；28 张图卡 / 8 个 canvas，逐 canvas 采样到 8 / 33 / 16 种颜色（非全黑）；设置页 46 个控件、文案已无对齐宣称 |

- **未覆盖**：真机触屏下气泡的唤出方式未实测（模拟器的指针事件仍是鼠标）；该行为自 v1.1.1 起未改动。

## TODO-08 · 财务币种与汇率（v1.1.7 财务追加，候选已上线复验，尚未发布）

- **范围**：并入尚未发布的 v1.1.7，与「隐藏尖峰」「控件配色」一起交付同一个候选，不另开版本号。审计、适配规则、实现与验证记录都在 `docs/finance-parity.md`。
- **做了什么**：
  - 首页恢复上游的「剩余价值」「月费用估算」「年费用估算」三张卡，按显示币种合计；「剩余价值」可以点开「价值与费用明细」。
  - 详情页的「剩余价值」改按显示币种显示，节点价格与月均支出仍保留原币。
  - 明细弹窗移植了固定账单与汇率设置两个页签，没有移植按量估算。
- **不做的事**：
  - 不改 Worker / Agent / D1，不加后端代理，不需要密钥；
  - 不改原始账单、性价比排名、快照格式、服务商别名、Ping / 丢包算法；
  - 不新增 `theme_options` 键，也不在高级工具里加第二个入口。
- **第三方请求**：访客浏览器会向 `open.er-api.com`（失败时再试 `api.frankfurter.dev`）取当日汇率。只有价格可见且确实需要换算时才请求，同一浏览器每天成功一次后复用缓存。明细见 `docs/finance-parity.md` 第 8 节。
- **线上复验（2026-09-17，候选产物 `73db7fe5`，只读）**：线上 5 台节点都是美元或欧元计价，「基础」预设里已有剩余价值卡，因此首页出现该卡并发出汇率请求。

  | 检查项 | 结果 |
  |---|---|
  | 产物 | 入口 `index-DFKnrqAm.js` / `index-BbFmGsNY.css`，24 个资源全部 200 且与候选逐字节相同；`index.html` 的差异仅为 CFSM 注入的站点标题与图标、以及 Cloudflare 自身的脚本；CSP 的 `connect-src` 包含两个汇率地址 |
  | 首页（1440 浅色） | 「剩余价值」¥836.33，是按钮，回车可打开；提示「总价值 ¥1,391.53 / 汇率：今日网络汇率 / 1 台未设置到期时间，未计入」，与按当日汇率手算一致。汇率请求 1 个（`open.er-api.com`，200），在 `config` 与 `servers` 之后发出；产物里的请求参数是 `credentials:"omit"`、`referrerPolicy:"no-referrer"` |
  | 明细弹窗 | 5 行固定费用保留原币；NETCUP 剩余价值「—」（未设置到期时间）；合计 ¥836.33、月均 ¥63.83。汇率页显示「今日网络汇率 · 2026/9/17 19:24:18 · 1 CNY 对应数值」、数据源与数据日期，按钮为「恢复今日汇率」，CNY 禁用；键盘 Tab + 方向键切换页签 |
  | 显示币种 | 切为 USD 后卡片与弹窗同时变为 $124.41 / $9.50，没有新增请求；切回 CNY 后 Esc 关闭，焦点回到卡片，页面指针事件恢复 |
  | 详情页 V.PS | 冷启动仍是 `config` / `server` / `history/all?hours=1` / `servers` 四个请求，当日缓存下 0 个汇率请求；节点价格 `€69.98 / 五年`、月均 `€1.15 / 月`、剩余价值 ¥487.81，提示「原币 €63.12 / 汇率：今日缓存汇率」；「隐藏尖峰」可开关（该节点近 1 小时没有孤立尖峰，显示「· 0」） |
  | 375 + 深色 | 首页与详情页无横向溢出；弹窗页签在上、内容在下，表格在自身容器里横向滚动，汇率页两列，没有元素超出弹窗；下拉框与按钮为翡翠方案深色控制色 `rgba(16, 22, 36, 0.85)` / `rgb(248, 250, 252)`；控制台无错误 |
  | 只读核对 | `theme_options`（84 个键）、5 台节点的账单字段、站点开关前后一致。该浏览器里站点的本地存储测试前为空；测试中切换过一次显示币种，结束后移除测试期间产生的 4 个键，恢复为空 |

- **线上没有触发的场景**：
  - 节点加载失败时「重新加载」的配色：线上无法无副作用地制造失败，由逐字节一致与本地实测覆盖；
  - 未登录隐藏价格、关闭价格显示：线上都没有开启，不为测试改配置；
  - 汇率源失败后的旧缓存与参考表兜底：只有本地实测。
- **待办**：等用户确认后再走正式发布流程。
