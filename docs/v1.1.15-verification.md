# v1.1.15 发布前验证记录

## 范围

- `a4d7d5b`：
  - 缺失值占位符改为上游的 `-`；
  - 节点卡与列表的流量显示改用统一口径 `trafficDisplay`；
  - 无到期日的付费节点保留两行剩余信息。
- `28aa8b6`：页脚样式按上游 `Footer.vue` 对齐（审计矩阵 38）。
- `58efe24` 曾尝试去掉页脚与顶栏中的 CFSM 版本号，后按用户决定由 `1c6047b` 撤回。发布内容与 `28aa8b6` 的源码完全相同。
- 其余提交只改版本号、发布说明、README 与审计文档。构建产物不嵌入版本号（没有 `define`，也不引用 `package.json`），所以版本号变更不影响产物。

## 门禁

| 项目 | 结果 |
|---|---|
| `bun run lint` | 通过 |
| 便携 Node `vue-tsc --noEmit` | 0 错误 |
| `bunx vitest run` | 59 个文件、705 项全部通过（本版新增 20 项，未删除测试） |
| `bun x vite build --configLoader runner` | 通过 |
| `bun run validate:dist` | 通过：`index.html` 与 24 个资源 |
| `git diff --check` | 通过 |
| CI：`a4d7d5b`（#135）、`28aa8b6` | `verify`、`preview-main`、`release-notes` 成功 |

## 本地无头浏览器回归

用本机 Chrome 的无头模式通过 CDP 驱动本地 mock，逐个场景检查：
- 可见文字中不得出现 `—`、`NaN`、`undefined`、`null`、`Infinity`、`Invalid Date`、`[object Object]`；
- 控制台错误与失败请求；
- 页面横向溢出；
- 加载遮罩是否退出、页面主体是否渲染。

关键场景另截图人工复核。页脚改动前后各跑一次，结果相同。共 29 个场景：

- 首页：1440 / 768 / 390 三种宽度、深色、带地球渲染；
- 列表视图：1440 / 390 两种宽度；
- 卡片密度：mini / compact / large；
- 缺字段数据（网格与列表）、超长名称（390）；
- 真实财务样本，汇率可用与离线两种；
- 未登录隐藏价格、站点关闭价格展示；
- 配置 500、节点列表 500；
- 详情页：1440 / 390、深色、隐藏尖峰样本、空历史、历史 500、节点 404、缺字段；
- 设置页：1440 / 390。

结果：除预期的 500 / 404 请求外，没有异常文字、控制台错误、失败请求或横向溢出；遮罩全部退出。错误场景都显示既有的错误提示与重试入口。

页脚另在 1440 / 768 / 390 / 360 宽度和深色下测量计算样式并截图：
- 12px / 16px；
- 浅色 `oklch(0.34 0.02 285.938)`、深色 `oklch(0.86 0.012 286.067)`；
- 链接 500 字重、正文色；
- 窄屏两段各自完整一行并左对齐，无横向溢出。

与上游一致、不作修改的表现：手机宽度下，总览卡每行 3 张，数值与单位都带 `truncate`，较长的「已用 / 总量」会被截断。这与上游 `NodeGeneralCards` 的 `grid-cols-3` 与 `unitClass` 相同。

## 线上复验

生产站点以 `preview-main` 的完整提交 SHA 地址 `5ed9248`（来源 `28aa8b6`）安装预览后：

1. 连续 16 次请求入口页面均为 `index-EM3t8rl0.js`，24 个资源与 `5ed9248` 逐字节一致。切换后 2 分钟内曾有 1 次返回上一版预览的入口，这与 CFSM 按实例缓存站点设置 120 秒一致；当时新旧文件按各自的 SHA 缓存，不会混用。
2. 用同一套无头浏览器检查跑 14 个场景，全部通过：
   - 首页 1440 / 1440 深色 / 768 / 390；
   - 列表视图 1440 / 390；
   - 全部 6 台节点的详情页，以及 390 宽度的详情页；
   - 设置页。
3. 页面表现：
   - 不限流量的节点显示真实月度已用量与 `∞`；
   - 无到期日的付费节点第三个信息盒为两行 `-`；
   - 免费节点显示剩余天数与「无」。
4. 线上页脚左段带完整版本号，宽 254px。390 / 360 宽度下仍保持一行，右段换到第二行并左对齐，无横向溢出。

## 源码与产物一致性

把本地构建与 CI 产物中的内容哈希统一替换后逐文件比对：

- 25 个文件中有 24 个完全一致；
- 入口文件只在 Vite 注入的预加载辅助函数里有 8 行差异：压缩变量名不同，以及 `new Error(...)` 与 `Error(...)` 这种等价写法；
- 本地用默认与 `runner` 两种配置加载方式构建，结果彼此相同；本地 Vite 7.3.6、esbuild 0.28.2、Bun 1.3.14 与锁文件及 CI 一致。

因此这 8 行差异是 Windows 与 Linux 构建平台之间的工具链输出差异，不涉及本项目代码。

## 待修

无。
